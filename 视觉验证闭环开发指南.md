# Zen Studio 视觉验证闭环开发指南

> **版本**: V2.0（项目专用简化版，直接交给 AI 工程师执行）
> **日期**: 2026-07-31
> **前身**: 通用版《GUI 视觉验证闭环开发指南》V1.0（7 个 prompt 模板）；本版按 Zen Studio 实战收敛
> **方法论出处**: `文档/修改记录/2026-0731-2208_聊天区选区高亮偏下修复与视觉验证闭环经验.md` §6

---

## 一、给 AI 工程师的一句话任务约束

> **改动任何 GUI 视觉效果时，必须走视觉验证闭环：基线截图 → mock 注入真实界面状态 → 修复 → 同脚本复测对比 → 用户真机自然操作终裁，未达标继续迭代。**

---

## 二、怎么看屏幕——三条「眼睛」通道

| 通道 | 机制 | 命令 / 用法 | 适用 |
|---|---|---|---|
| **IDE 自动截图** | 主窗口 QTimer 定时 `grab()` 落 PNG，AI 读最新帧即「看到」真机画面 | `uv run main.py --auto-screenshot --screenshot-interval 1`（可加 `--screenshot-on-start` 首帧即截）；产物 `.tmp/screenshot_YYYYMMDD_HHMMSS_<序号>.png`，终端打 `[screenshot] <路径>` | 真机走查、布局核对、用户反馈定位 |
| **offscreen 控件级截图** | 无头起 QApplication，真实实例化生产控件后 `widget.grab()` 出图 | `QT_QPA_PLATFORM=offscreen uv run python <脚本>`；脚本写 `.temp/` | 无显示器/CI、控件级验证、**双帧差分度量** |
| **像素扫描** | 对 QImage 逐行扫像素做定量断言，代替「看着差不多」 | offscreen 脚本内 `widget.grab().toImage()` 逐行对比 | 选区带、间距、对齐类度量 |

**offscreen 能代表真机的前提**：探针须复现真机级联环境——给 QApplication 挂应用级 qss
（`QMainWindow, QDialog { selection-background-color: ... }`，与 `assets/themes/base.qss`
同款继承规则）。不挂则控件级 qss 行为测不准，offscreen 与真机不一致。

---

## 三、怎么量——offscreen 双帧差分度量法（首选武器）

以 `.temp/verify_input_selection.py` 为模板：

1. 同一控件先**无选区** `grab()` 一帧，再程序置选区 `grab()` 一帧；
2. 逐行对比两帧像素，差异行即选区带行 → 得带纵向跨度 `[bs₀, bs₁]` 与横向范围；
3. 回无选区帧、在带横向范围内扫暗像素（RGB 和 < 300）→ 得字形墨盒跨度 `[is₀, is₁]`；
4. 定量断言：上留白 = `is₀−bs₀`，下留白 = `bs₁−is₁`，中心偏差 = 两跨度中点差；
   阈值参考 `|上下留白差| ≤ 2` 且 `|偏差| ≤ 1px`。

**修复前先跑探针拿「复现证据」，修复后用同一脚本复测归零**——同脚本前后对照是最强证据。

---

## 四、怎么动——XTEST 真机驱动（有坑，谨慎启用）

- 依赖：`python-xlib`（dev 组常驻依赖，仅 `.temp/` 脚本 import，不进打包产物，见 `pyproject.toml`）。
- **坑 1 坐标**：python-xlib `translate_coords` 在窗口被 WM reparent 后给出错误负坐标。
  解法：`xwininfo -name "窗口名"` 取 `Absolute upper-left X/Y` 作基准原点。
- **坑 2 中文输入**：XTEST 逐键打 ASCII 会被 fcitx 拦截转成预编辑文本。
  解法：独立 PySide 小进程把中文写入 CLIPBOARD 并存活保权，随后只发 `ctrl+v`（`.temp/xpaste.py`）。
- **坑 3 侵入性（致命）**：真机驱动在用户活跃桌面抢鼠标键盘。**裁定：仅限无头会话或用户
  不在场时启用；用户在场时，真机环节必须退化为「用户自然操作」。**

---

## 五、怎么收敛——两级闭环

- **第一级（机器）**：offscreen 双帧差分「同脚本前后对照」+ 存量冒烟测试防回归；
- **第二级（人）**：offscreen 度量无法替代人眼终裁，真机最后一环由**用户日常使用中顺手确认**收口。

---

## 六、项目纪律（AGENTS.md 约束，验证环节同样适用）

1. 一律用项目 `.venv`：`uv run ...`，禁止系统全局 Python；
2. 临时产物只放项目内：脚本放 `.temp/`，截图放 `.tmp/`，严禁 `/tmp` 等系统目录，不入库；
3. 主题键零新增：选区带/高亮色复用现有令牌（如 `accent` / `link_fg`），不得新造主题键；
4. mock 数据每轮迭代保持一致，否则截图对比失去意义；
5. 截图命名带版本号（`screenshot_v1_base.png` / `screenshot_v2_mock.png`），方便人工回溯。

---

## 七、流程教训（踩过的坑，直接跳过）

1. **先问清痛点控件再动手**：视觉类问题立项前先请用户指认/截图圈出具体控件，
   避免「修对了没人抱怨的地方」（2208 计划教训：立项按推测修了 AI 输出框，
   真机痛点其实在人类输入框）。
2. **真机驱动默认不用**：用户在场时直接走「offscreen 度量 + 用户自然确认」两级闭环，
   省去 XTEST 的坐标/IME/侵入性三坑。

---

## 八、迭代记录

| 版本 | 日期 | 变更内容 |
|---|---|---|
| V1.0 | 2026-07-31 | 通用版：7 个 prompt 模板 + 选型速查表 |
| V2.0 | 2026-07-31 | 项目专用简化版：删模板库，固化三条眼睛通道、双帧差分度量法、XTEST 坑存档、两级闭环与项目纪律，可直接交付 AI 工程师执行 |

---

*本文档为活文档，随实践反馈持续迭代。*
