# GUI 视觉验证闭环开发指南（项目通用版）

> **版本**: V2.1（项目通用版，可直接交给任何 AI/人类工程师执行，不限于 IDE 类项目）
> **日期**: 2026-08-07
> **前身**: V2.0（Zen Studio 项目专用版）；V1.0（通用版 7 个 prompt 模板）
> **方法论出处（实战案例存档）**:
> - `文档/修改记录/2026-0731-2208_聊天区选区高亮偏下修复与视觉验证闭环经验.md` §6（三层通道 / 双帧差分 / 真机驱动三坑 / 两级闭环）
> - `文档/修改记录/2026-0803-0741_思维链卡body空白缺陷视觉闭环修复计划.md`（五拍闭环 / 冒烟盲区教训 / 修前修后成对截图核验）
> - `文档/提示语句/自动截图功能实现提示词.md`（目标应用无截图通道时的加装方案）

---

## 一、给工程师的一句话任务约束

> **改动任何 GUI 视觉效果时，必须走视觉验证闭环：先指认痛点 → 基线截图取证 → mock 注入真实界面状态复现 → 修复 → 同一脚本复测对比 → 回归补强 → 用户真机自然操作终裁，未达标继续迭代。不允许「改完代码看着差不多就交付」。**

核心原则：

1. **眼见为实**：视觉问题靠截图证据说话，不靠读代码臆断。
2. **同脚本前后对照**：修复前跑一遍探针拿「复现证据」，修复后用**同一个脚本**复测——这是最强证据。
3. **逻辑断言 ≠ 像素可见**：测试只断言数据状态（文本存在、标志位为真）拦不住「内容在但看不见」的缺陷，必须补像素级断言。

---

## 二、适用场景

- 任何有图形界面的软件：桌面应用（Qt / Electron / Tauri / WPF / Swing / 原生）、游戏（Pygame / Unity 编辑器工具）、嵌入式 UI、Web 页面等。
- 典型问题类型：对齐/间距/留白异常、选区或高亮位置偏移、内容存在但渲染不可见（空白/裁切/高度锁死）、主题换肤后颜色不可读、缩放/DPI 异常、布局溢出。

---

## 三、怎么看屏幕——三条「眼睛」通道

| 通道 | 机制 | 适用 |
|---|---|---|
| **① 应用内自动截图** | 应用主循环挂定时器，定时把窗口/画布抓帧落盘 PNG，工程师读最新帧即「看到」真机画面 | 真机走查、布局核对、用户反馈定位 |
| **② 无头（offscreen/headless）控件级截图** | 不起真实窗口，用无显示模式实例化**生产控件**（不是简化替身），抓帧出图 | 无显示器/CI 环境、控件级验证、**双帧差分度量** |
| **③ 像素扫描** | 对抓帧得到的图像逐行/逐区域扫像素做定量断言，代替「看着差不多」 | 选区带、间距、对齐、可见性类度量 |

### 3.1 通道①：应用内自动截图（加装规范）

目标应用若没有截图通道，先给它加一个（一次性小投入，全程受益）。规范要点：

- 命令行参数控制：`--auto-screenshot`（开关）、`--screenshot-interval N`（每 N 秒一帧）、`--screenshot-on-start`（首帧即截）。
- 产物命名：`screenshot_YYYYMMDD_HHMMSS_<帧序号>.png`，落项目内临时目录（如 `./.tmp/`），终端打印 `[screenshot] <路径>` 方便定位最新帧。
- 抓帧 API 按渲染框架选：
  - Qt（PySide/PyQt/C++）→ `widget.grab().save(path)`
  - Pygame → `pygame.image.save(screen, path)`
  - Web/Electron → Playwright/Puppeteer `page.screenshot()` 或 Electron `capturePage()`
  - OpenCV/视频流 → `cv2.imwrite(path, frame)`
  - 其他框架 → 用对应框架的抓屏/离屏渲染 API
- 截图用计时器或帧计数触发，**不得阻塞主循环**。

### 3.2 通道②：无头截图能代表真机的前提（关键易漏）

无头探针必须**复现真机的级联环境**，否则测不准、结论不可信：

- Qt 项目：给 QApplication 挂应用级 qss（与生产主题文件同款继承规则，如全局 `selection-background-color`），再实例化控件。不挂则控件级样式在无头环境的行为与真机不一致。
- Web 项目：headless 浏览器要加载生产 CSS/主题、设置与真机一致的 viewport 与 devicePixelRatio。
- 通用规则：**无头环境缺失的任何全局状态（主题、字体、DPI、环境变量）都要在探针里补齐**，并在报告中注明「探针环境与真机的差异清单」。

无头方式参考：Qt 用 `QT_QPA_PLATFORM=offscreen`；Web 用 Playwright headless；其他框架查其离屏渲染/无头模式能力。

### 3.3 通道③：像素扫描

- 对图像逐行比较两帧差异、或在指定区域内统计目标颜色像素，把「视觉判断」变成「数值断言」。
- Qt 直接 `widget.grab().toImage()` 逐行读像素；Web 可对截图 PNG 用 Pillow 逐行扫描；任何框架都可走「截图 → Pillow/NumPy 分析」的通用路径。

---

## 四、怎么量——双帧差分度量法（首选武器）

适用于「某元素在状态 A 出现、状态 B 不出现」的一切视觉度量（选区、高亮、悬浮态、展开态、焦点框等）：

1. 同一控件先取**状态 B（无目标元素）** 抓一帧，再程序切换到**状态 A（有目标元素）** 抓一帧；
2. 逐行对比两帧像素，差异行即目标元素所在行 → 得元素纵向跨度与横向范围；
3. 回状态 B 帧、在元素横向范围内扫特征像素（如暗像素代表字形墨盒）→ 得基准内容跨度；
4. 定量断言。以「选区高亮是否居中」为例：上留白 = `墨盒上缘 − 高亮带上缘`，下留白 = `高亮带下缘 − 墨盒下缘`，中心偏差 = 两跨度中点差；阈值参考 `|上下留白差| ≤ 2px` 且 `|偏差| ≤ 1px`。
5. **修复前先跑探针拿「复现证据」，修复后用同一脚本复测归零**——同脚本前后对照是最强证据。

阈值按项目实际定（字号、DPI、设计规范），但**必须落成数值**，不允许「目测差不多」。

---

## 五、怎么造状态——mock 注入真实界面状态

视觉缺陷往往只在特定数据流下出现（流式帧序列、长文本折行、空态/满态）。复现要求：

1. **mock 真实数据流**：不是随手填两行假数据，而是按生产事件序列注入（例如流式协议的三帧流 + 结束帧 + 用户交互），覆盖缺陷报告中的真实场景；
2. **实例化生产控件**：探针里 new 的是真正的生产类，不是简化替身，否则验证无效；
3. **mock 数据每轮迭代保持一致**：修复前后喂同一份数据，否则截图对比失去意义；
4. **多变体覆盖**：主题换肤、字号档位、长文本/空文本等边界变体各补一帧。

---

## 六、怎么动——真机驱动（有坑，谨慎启用）

把键鼠事件程序注入真实桌面（Linux XTEST / Windows SendInput / macOS CGEvent / Web 的 CDP）可以驱动真机，但实战踩过三个坑：

- **坑 1 坐标**：窗口被窗口管理器 reparent 后，库内坐标换算可能给出错误负坐标，点击全落空。
  解法：用系统工具取窗口绝对坐标（Linux：`xwininfo -name "窗口名"` 的 `Absolute upper-left X/Y`）作基准原点。
- **坑 2 输入法（IME）**：逐键注入 ASCII 会被输入法拦截转成预编辑文本，中文/日文等输入必踩。
  解法：独立小进程把目标文本写入系统剪贴板并存活保住剪贴板所有权，随后只发 `Ctrl+V`。
- **坑 3 侵入性（致命）**：真机驱动在用户活跃桌面上抢鼠标键盘，用户会直接关掉被驱动的窗口。
  **裁定：真机驱动仅限无头会话或用户不在场时启用；用户在场时，真机环节必须退化为「用户自然操作」。**

---

## 七、怎么收敛——两级闭环

- **第一级（机器）**：无头双帧差分「同脚本前后对照」+ 存量冒烟/回归测试防回归；
- **第二级（人）**：无头度量无法替代人眼终裁。真机最后一环由**用户日常使用中顺手确认**收口（用户一句「成功了」即闭环）；不要把「请用户专门配合测试」作为默认路径。

---

## 八、执行流程：五拍闭环（标准作业程序）

| 拍 | 动作 | 产出 |
|---|---|---|
| **T1 复现截图（修前）** | 写无头探针，mock 真实状态，抓修前截图 + 打印数据态（尺寸/可见性/内容长度） | 修前视觉证据 PNG + 数据日志 |
| **T2 视觉查看（修前）** | 用图像查看能力（多模态 AI 或人工）逐张看修前截图，亲眼确认缺陷现象，与数据日志互证 | 缺陷确认记录 |
| **T3 修复** | 定位根因并修改（改动面尽量收敛单点）；编译/语法检查通过 | 代码改动 |
| **T4 复验截图（修后）** | **同一脚本**重跑，产出修后截图；逐张确认「缺陷现象 → 预期现象」；边界变体各补一帧（主题/长文本/限高封顶等） | 修前/修后成对截图 |
| **T5 回归补强** | 给存量测试**补像素级断言**（不只断逻辑状态）；全量回归复跑 | 新增断言 + 回归全绿 |

完成后进入第二级闭环：用户真机自然使用确认。

---

## 九、工程纪律（通用约束，验证环节同样适用）

1. **用项目自己的运行环境**：项目虚拟环境/锁文件（如 `.venv`、`uv run`、`npm ci`），禁止用系统全局环境跑探针——版本不一致会让截图结论失真；
2. **临时产物只放项目内**：探针脚本放 `./.temp/`，截图放 `./.tmp/`（或项目约定的同类目录），严禁系统临时目录（`/tmp`、`$TMPDIR` 等），不入版本库；
3. **设计令牌零新增**：修复用的颜色/间距复用现有设计令牌（主题变量），不得新造；
4. **mock 数据每轮迭代保持一致**（见 §五）；
5. **截图命名带版本号**：`xxx_v1_base.png` / `xxx_v2_mock.png` 或 `xxx_pre.png` / `xxx_post.png`，方便人工回溯对照；
6. **验证脚本不进产物**：探针依赖（如截图/注入库）只进 dev 依赖组，生产代码零引用，打包不收编。

---

## 十、流程教训（踩过的坑，直接跳过）

1. **先问清痛点控件再动手**：视觉类问题立项前先请用户指认/截图圈出具体控件。实战教训：按推测修了 AI 输出框，真机痛点其实在人类输入框——「修对了没人抱怨的地方」。
2. **真机驱动默认不用**：用户在场时直接走「无头度量 + 用户自然确认」两级闭环，省去真机驱动的坐标/IME/侵入性三坑（见 §六）。
3. **逻辑断言有盲区**：冒烟测试只断言逻辑状态（内容存在、开关为真）拦不住「内容在但渲染不可见」的缺陷（实战案例：文本在控件里 45 字符、可见标志为真，但容器高度被锁死一行，用户看到一片空白）。**每修一个视觉缺陷，就补一条像素级断言**永久填堵。
4. **无头环境不挂全局环境 = 白测**：无头探针不复现真机级联环境（主题/字体/DPI），结论与真机不一致（见 §3.2）。
5. **修前不修后成对截图，缺一张都不叫闭环**：只有修后截图没有修前截图，无法证明「修的是这个缺陷」。

---

## 十一、迭代记录

| 版本 | 日期 | 变更内容 |
|---|---|---|
| V1.0 | 2026-07-31 | 通用版：7 个 prompt 模板 + 选型速查表 |
| V2.0 | 2026-07-31 | 项目专用简化版：删模板库，固化三条眼睛通道、双帧差分度量法、真机驱动坑存档、两级闭环与项目纪律 |
| V2.1 | 2026-08-07 | 项目通用版：去除 IDE/Qt 专属绑定，泛化为任意 GUI 软件可执行；新增自动截图加装规范（§3.1）、无头环境代表性前提（§3.2）、mock 注入真实状态规范（§五）、五拍闭环 SOP（§八，源自 0741 实战）、冒烟盲区与成对截图教训（§十 3/5）、跨框架抓帧 API 对照 |

---

*本文档为活文档，随实践反馈持续迭代。*
