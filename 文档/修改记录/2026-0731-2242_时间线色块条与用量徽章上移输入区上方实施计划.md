> ⚠️ **归档声明**：本文档为历史快照，记录了特定时间点的决策与实施状态。
> 当前代码状态以对应 `.py` 源文件为准，本文档内容可能已过时，仅供参考。

> **状态**：✅ 已实施（2026-07-31 实施并验证，见文末实施记录；2026-08-01 归档，自 `work plans/` 移入）
> **范围**：`gui/panels/chat/panel.py`（布局装配调整）；预期零改动 `tabs.py` / `timeline.py` / `theme.py`
> **时间**：2026-07-31 22:42（创建，UTC+8）
> **优先级**：中
> **上游选型**：`../选型记录/2026-0731-2232_时间线色块条与用量徽章布局位置选型.md`（方案 F 定夺）

# 时间线色块条与用量徽章上移输入区上方实施计划

> 按选型定夺的方案 F：将**会话活动时间线色块条**（现位于聊天卡片最底部、
> 输入对话框之下）与**上下文用量徽章**（现位于输入区底行、模型下拉与发送
> 按钮之间）合并为**一行状态条**，上移到**输出区与输入区之间**——左条
> 右徽，徽章独立右侧恒宽。每标签一套实例，语义零迁移。

## 1. 目标布局（已与用户图形化确认）

```
├─ 输出区（splitter 上段）───────────┤
│                                    │
├─ 状态行 ───────────────────────────┤  ← 新位置
│ ▓▓▓▓▓▓░░▓▓▓░░（时间线条）  [~42%]  │     左条右徽，徽章恒宽右对齐
├─ 输入区（splitter 下段）───────────┤
│ ┌─ 输入框 ────────────────────┐    │
│ └─────────────────────────────┘    │
│ [模型▾][版本▾]          [ 发送 ]   │  ← 底行移除徽章，纯操作行
└────────────────────────────────────┘
     （卡片最底部不再有时间线条）
```

## 2. 现状盘点（实证，2026-07-31）

`gui/panels/chat/panel.py`：

- `_build_layout()`：`card_layout.addWidget(self._splitter, 1)` 后
  `card_layout.addWidget(self.timeline)`——时间线条挂卡片底部常驻
  （1824 计划 D2，不进 splitter 因子件会被拖拽均分）。
- `_build_input_box()`：`button_row` 内 `model_bar → _usage_label →
  stretch → _send_button`；`_usage_label` 恒宽（`~100%` + 8px）、
  AlignCenter、空文本占位——显隐不改 sizeHint 的左栏宽度病根纪律
  （2026-0724-2305 教训）。
- splitter 垂直双向：output / input_box 比例可拖拽、状态持久化
  （DEFAULT_SPLITTER_SIZES）。

## 3. 设计要点

### 3.1 状态行挂载点（关键决策）

状态行**不进 splitter**（子件会被拖拽均分，1824 计划 D2 已实证此约束），
也**不再挂卡片底部**。落点为 **input_box 容器内、输入框上方**——
`_build_input_box()` 的 box_layout 首行：

```
box_layout: [状态行(定高)] → [输入框(stretch 1)] → [button_row]
```

效果等价「输出区与输入区之间」：状态行随 splitter 拖拽与输入区整体移动，
视觉恒在输出区下缘、输入框上缘之间；定高不参与拉伸。

### 3.2 同行装配

- 顺序：**左时间线条（stretch 1）→ 右用量徽章（恒宽）**。
- 行高由时间线条决定（约 30px，MAX_H 26 + 上下各 2，1824 已定）；
  徽章垂直方向随 Qt 布局默认居中。
- 徽章**独立右侧**，不嵌入条尾——保留 `~100%` 恒宽占位与 AlignCenter
  语义不变，时间线条可用宽度不受徽章影响。

### 3.3 不变项（明确冻结）

- 时间线条数据源、清条点（异后端切换新会话）、主题三键、`set_colors`
  注入——1824 计划已落地，本计划零改动。
- 徽章簿记、usage_update 路由、tooltip 明细、热态变色、清零点
  （`reset_session` 语义）——1412/1454 计划已落地，本计划零改动。
- 底行其余元素（ModelBar 双下拉、stretch、发送/停止双态按钮）顺序不变。
- 主题键零新增、间距复用卡片现有 spacing 体系。

## 4. 决策表

| # | 决策点 | 定夺 | 依据 |
|---|---|---|---|
| D1 | 状态行挂载点 | **input_box 内、输入框上方** | splitter 子件均分约束（1824 D2 实证）；随输入区拖拽移动，视觉恒在输出/输入分界 |
| D2 | 徽章摆法 | **独立右侧恒宽**，不嵌入条尾 | 用户确认「左条右徽」；恒宽占位纪律不动；条宽不被蚕食 |
| D3 | 行高与对齐 | 行高随时间线条（约 30px），徽章布局默认垂直居中 | 不为徽章单独撑行高 |
| D4 | 卡片底部 | **移除 timeline 挂载**，卡片 = splitter 单件 | 方案 F 定夺 |
| D5 | 生命周期 | 不变（每标签实例，随会话清条/清零） | 方案 F 语义零迁移承诺 |
| D6 | 主题/样式 | 零新增键；badge 在状态行的视觉与底行一致（qss 选择器 #chatUsageLabel 按 objectName 命中，与父容器无关） | 最小改动面 |

## 5. 任务分解

| # | 任务 | 文件 | 要点 |
|---|---|---|---|
| T1 | 状态行装配 | `gui/panels/chat/panel.py` | `_build_input_box()`：box_layout 首行插入状态行容器（QHBoxLayout：timeline stretch 1 + `_usage_label` 恒宽）；`_build_layout()` 移除 `card_layout.addWidget(self.timeline)`；button_row 移除 `_usage_label`；实例化顺序微调（timeline/usage label 需在 _build_input_box 之前就绪） |
| T2 | 注释与文档串更新 | `gui/panels/chat/panel.py` | `_build_layout`/`_build_input_box` docstring 更新为方案 F 语义（状态行位置、徽章归属、1824 D2 引用更新）；file docstring 若涉布局描述同步 |
| T3 | 验证 | `.temp/`（不入库） | 见 §6 |

预期 `tabs.py` / `timeline.py` / `theme.py` / qss **零改动**——若实施中
发现必须触碰（如状态行间距需 qss 微调），在实施记录中如实补记。

## 6. 验证方案

1. **布局断言冒烟**（`.temp/smoke_status_row.py`，offscreen）：实例化
   ChatPanel（mock provider），断言——timeline 与 `_usage_label` 的父
   链归属 input_box 状态行；卡片布局不再直挂 timeline；button_row 子件
   为 model_bar + stretch + send 三件；拖拽 splitter 后状态行随输入区
   移动且高度不变；`_usage_label` 宽度恒定（~100% 占位语义回归）。
2. **既有冒烟回归**：`.temp/test_timeline.py`（30 项）/
   `.temp/smoke_timeline.py`（11 项）全过——分接/清条/着色逻辑未动，
   仅挂载点迁移，若冒烟含布局断言则同步更新；`.tmp/check_theme_tokens.py`
   主题校验全过。
3. **截图走查**（遵循 `视觉验证闭环开发指南`）：同一 mock Chunk 序列
   改造前后对比——`.temp/shots/status_row_before.png`（现状：条在卡片
   底部、徽章在底行）→ `status_row_after.png`（状态行在输出/输入之间、
   左条右徽、底行纯净）→ `status_row_splitter_dragged.png`（splitter
   拖拽后状态行跟随输入区）；亮暗主题各截一轮复核徽章与色块条配色。
4. **真机走查**：kimi 后端跑一轮多工具任务，实证时间线照常上条、徽章
   照常更新、tooltip 明细正常；busy/停止/新会话清条清零语义不变。

## 7. 不做的事

- **不改**时间线条与徽章的数据/着色/生命周期逻辑（1824、1412/1454
  已落地，纯布局迁移）。
- **不动** splitter 持久化机制与 DEFAULT_SPLITTER_SIZES 语义（状态行
  进 input_box 后输入区最小高度略增，属自然结果，不调参）。
- **不做**徽章嵌入条尾、点击跳转、联动高亮等增值项（选型未含）。
- **不动** `tabs.py` / `timeline.py` / `theme.py` / qss（预期零改动，
  如有偏差实施记录补记）。

## 8. 与既有文档关系

- 上游选型：`文档/选型记录/2026-0731-2232_时间线色块条与用量徽章布局
  位置选型.md`（方案 F 定夺，A/B/C/D/E 存档备查）。
- 挂载点约束继承 1824 计划 D2 实证结论（splitter 子件均分）——本计划
  是 1824 D2 挂载位置的**迁移修订**（卡片底部 → input_box 顶部），
  1824 文档维持归档快照不回头改。
- 徽章恒宽占位纪律继承 2026-0724-2305 左栏宽度病根治理，迁移后语义不变。

---

## 9. 实施记录（2026-07-31 当晚完成）

决策表 D1~D6 全部按定夺落锤，无翻案。改动面与预期一致——
**仅 `gui/panels/chat/panel.py` 一个文件**（`_build_layout` 移除卡片底部
timeline 挂载；`_build_input_box` 首行新增状态行 QHBoxLayout：timeline
stretch 1 + `_usage_label` 恒宽；button_row 移除徽章；file docstring 与
徽章小节注释同步方案 F 语义）。`tabs.py` / `timeline.py` / `theme.py` /
qss **零改动**，符合预期。

实施偏差如实补记：

1. **验证 §6-3「亮暗主题各截一轮」修正**：项目无暗主题（list_available_themes
   = cloud/wheat/sky/mint 四亮色），截图走查改 cloud + sky 双主题。
2. **status_row_before.png 未重复生成**：改造前基线沿用 1824 计划的
   `timeline_after.png`（同一代码旧态：条在卡片底部、徽章在底行），
   同一 mock 序列可直接对照。

验证闭环（全部通过）：

- `.temp/smoke_status_row.py`（新建）布局断言 **15 项**：状态行为
  input_box 首件 / 左条右徽顺序与 stretch / 底行三件纯净（model_bar +
  stretch + send）/ 卡片布局仅 splitter 单件 / timeline 父链 /
  **splitter 拖拽后状态行随输入区上移且高度不变** / 徽章空文本·有文本
  恒宽一致 / 50% 上屏。
- 回归：`.temp/test_timeline.py` 30 项、`.temp/smoke_timeline.py` 11 项、
  `.temp/smoke_chat_activity.py` 13 项全绿（分接/清条/着色逻辑零改动）；
  `.tmp/check_theme_tokens.py` 主题校验全过。
- 截图走查（`.temp/shot_status_row.py`，同一 mock 序列 + mock 42% 用量）：
  `status_row_after.png`（常规，状态行居输出/输入分界、底行纯净）/
  `status_row_splitter_dragged.png`（输入区拉高，状态行随之上移）/
  `status_row_sky.png`（第二主题配色复核），各附 `_strip` 状态行 3 倍
  局部放大——观感符合 §1 目标布局。

真机走查（§6-4 kimi 后端多工具任务）留待日常使用自然验证。

*创建于 2026-07-31 22:42 (UTC+8) | 已实施于 2026-07-31*
