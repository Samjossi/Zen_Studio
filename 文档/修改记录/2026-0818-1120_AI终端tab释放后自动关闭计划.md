# AI 终端 tab release 后自动关闭计划

> **状态**：已实施并验证（2026-08-18，真机复核通过）；同日追加结束后停留补丁（见 §6，offscreen 冒烟 17 项 ×3 连跑全过，待真机复核）
> **时间**：2026-08-18 11:20（UTC+8，编写）
> **起因**：ACP 反向能力落地后，agent 每条 Bash 命令开一个 `🤖` tab 且**永久残留**——`_release` 只清协议簿记、刻意留 tab（`agent_bridge.py:192` 注释「tab 保留供用户查看」，2026-08-18 晨间决策）。AI 活跃时标签栏被退出态 AI tab 淹没（用户实机截图实证），淹没用户自有终端。
> **决策**：2026-08-18 用户选定「release 后自动关闭」方案（替代"用户/AI 分区"方案——分区治标不治本，tab 只增不减的根因是只开不关）。
>
> **实施验收**（2026-08-18）：T1 `panel.py` 新增公开 `close_agent_session(session_entry)`（按会话对象定位复用 `_close_tab`，已手关则 no-op）；T2 `agent_bridge.py` `_release` 清账后封送 GUI 线程关 tab，docstring 同步新语义。offscreen 冒烟扩至 13 项 ×3 连跑全过（release 自动关 tab / 用户 tab 不受扰 / 手关后补 release no-op / kill+release 序列 / 死讯残留清理）；spike 回归 kimi 6/6 协议无回归。真机复核（2026-08-18 用户确认通过）：两轮 ×20 个后台 Bash 倒计时任务（35~80s 错开），🤖 tab 随命令完成逐个自动消失、无一残留，用户自有终端不受扰、无报错。

---

## 1. 核心判据

agent 调 `terminal/release` 的协议语义即「该终端我不再查询」——此后不会再有 `output()`/`wait_for_exit()` 请求（输出已在 release 前经尾部缓冲交付 agent，聊天区 Bash 工具卡另有留档）。**release 即关 tab 协议上无损**，执行期间 tab 照常可见、可 Ctrl+C、可手关，能力零回退。

## 2. 改动点（两文件，均小改）

### T1 面板新增公开关闭入口（`gui/panels/terminal/panel.py`）

```python
def close_agent_session(self, session_entry: _Session) -> None:
    """AI 桥 release 后自动关 tab：按会话对象定位（用户可能已手关，
    找不到即 no-op）；复用 _close_tab 既有路径（terminate 幂等 +
    session_closed 信号 + 空状态归零）。"""
```

- 在 `self._sessions` 里查 `session_entry` 索引，命中则 `_close_tab(idx)`，未命中直接返回
- 放在「公开接口」分区（`panel.py:314` 一带），供桥调用，不暴露 `_close_tab` 索引语义

### T2 桥 release 挂接自动关闭（`gui/panels/terminal/agent_bridge.py`）

- `_release`（`agent_bridge.py:192`）：簿记 pop、`_flush_waits` 之后，若 `rec.entry is not None`，`self._invoke_gui(lambda: self._panel.close_agent_session(rec.entry))` 关 tab
- 更新 `_release` docstring：删除「tab 保留供用户查看」旧语义，改为「release 即关 tab（协议语义：agent 不再查询）；执行期可视性不变」
- 更新类/模块 docstring 中「可手关 tab（等价 kill+release）」相关描述（语义不变，但 release 不再留 tab）

## 3. 边界与安全性核查（实施时逐条过）

| 场景 | 预期 |
| --- | --- |
| 正常流（create → 退出 → wait → release） | tab 在 release 时消失；输出已在 release 前交付 agent |
| kill → release | kill 只杀进程不动 tab，release 时才关（行为收敛于单一点） |
| 用户已手关 AI tab，agent 后补 release | `close_agent_session` 找不到 entry，no-op 不炸 |
| release 关 tab 触发 `session_closed` → `_on_tab_closed` | 簿记已 pop，查找落空，no-op（既有逻辑天然兼容） |
| 关闭的是当前可视 tab | `_close_tab` 既有逻辑切邻居 tab / 全关进空状态，无需特判 |
| 会话已退出再 `_close_tab` | `PtySession.terminate` 幂等，安全 |
| 用户自有「终端N」tab | 永不经 bridge 路径，不受影响 |

## 4. 非目标（本轮不做）

- agent 不调 release 的残留（连接死亡时 `on_connection_dead` 只兜底 wait，tab 仍留）——属异常路径，另行评估
- 用户/AI 标签分区 UI（已否决，见起因）
- ~~延迟关闭 / 设置开关（若实机观感"关得太快看不及"再加，先保持最简）~~ —— 2026-08-18 实机触发该预留条件，已落地为「结束后停留」补丁（见 §6），且时长用户可调（设置中心「终端」页，默认 4 秒）

## 5. 验证

1. **offscreen 冒烟**：扩 `.temp/test_agent_bridge.py`——release 后 tab 消失且 `count_tabs` 归零/减一、用户 tab 不受影响、手关后补 release 无异常、kill+release 序列正常；连跑 ×3
2. **spike 回归**：`scripts/spike_terminal_capability.py` 五 agent 复跑，确认协议行为无回归
3. **真机复核**：kimi 链路跑一轮多 Bash 命令任务，目检 tab 随命令完成逐个消失、用户终端不受扰 —— ✅ 已通过（2026-08-18，两轮 ×20 个倒计时任务实机确认）

---

## 6. 追加：结束后停留补丁（2026-08-18，实机反馈触发 §4 预留条件）

> **起因**：真机复核后发现大量 AI Bash 命令极短（<0.5s），tab 瞬生瞬灭、标签栏闪烁。用户拍板加停留机制、默认 4 秒、**用户可调**（设置中心「终端」页）。
> **语义演进（同日两轮实机反馈）**：初版为「最短存活」——`关闭时刻 = max(创建时刻 + N, release 时刻)`，只兜短命令；用户实机跑约 11 秒的倒计时命令发现结束后仍瞬关，确认真实需求是「结束后停留」——`关闭时刻 = release 时刻 + N秒`，**长短命令一视同仁**，结束后都停留 N 秒让用户看清结果。代价（AI 连续短命令时标签栏短暂堆叠几个待关闭 🤖 tab）经用户确认接受。**0 = 立即关闭**（回退补丁前行为）。

### 改动点

**T3 桥延迟关（`gui/panels/terminal/agent_bridge.py`）**

- `_release` 关 tab 封送 GUI 线程走 `_schedule_close`：停留时长为 0 → 立即 `close_agent_session`（原路径）；否则 `QTimer.singleShot(N秒, panel, close)` 停留满再关
- 时长存实例属性 `_close_delay_s`：构造时自读持久化初值，`set_close_delay_seconds()` 公开 setter 供 MainWindow 即时推送（读写均只在 GUI 线程，无竞态）
- 「最短存活」初版的 `created_at` 簿记与 max 计算已随语义切换移除（简化）
- `panel.close_agent_session` / 模块 docstring 同步新语义（定位/no-op/复用 `_close_tab` 逻辑全不变）

**T4 设置键（`gui/settings.py`，第 11 个固定键）**

- `KEY_TERMINAL_AI_TAB_CLOSE_DELAY_S = "terminal_ai_tab_close_delay_s"`；AppSettings / AppSettingsPatch / DEFAULT_SETTINGS 登记，默认 4
- 值域常量 `TERMINAL_AI_TAB_CLOSE_DELAY_MIN_S/MAX_S = 0/30` 与默认值常量化（设置中心 SpinBox 与 load_settings 防御共用单一来源）
- `load_settings` 值域防御：非 int（bool 亦拒）回默认；越界钳制 0~30
- 键演进：初版 `terminal_ai_tab_min_visible_s`（最短存活语义）随语义切换改名，旧键按既有机制读取即丢弃、自然消亡，无需迁移代码

**T5 应用链（`gui/main_window.py` + `gui/settings_dialog.py`）**

- MainWindow 自持 `_agent_terminal_bridge` 引用；新槽 `set_terminal_ai_tab_close_delay(seconds)`：持久化 + 即时下发桥（无需重启；已在停留倒计时中的 tab 按旧值到点关）+ 设置中心 sync + 状态栏反馈；`reset_settings` 即时重应用链补该项（重置回 4 秒）
- 设置中心「终端」页新增「AI 终端标签结束后停留时长」SpinBox（0~30 秒，后缀「秒」）+ hint 说明行；`_reload_terminal` 回读（`_reloading` 抑制防回环）；页副标题更新为「终端按键与 AI 标签行为配置」

### 关键实现约束（冒烟实证踩坑）

`QTimer.singleShot(ms, context, fn)` 会在**调用线程**先 `startTimer`，reader 线程无事件分发器 → 定时器静默建不起来（`QObject::startTimer: ... event dispatcher has already been destroyed`，tab 永不关闭）。必须在 GUI 线程创建定时器，故延迟调度整体经 `_invoke_gui` 封送；receiver=面板，面板销毁则定时器自动取消。

### 边界（沿用原方案，均 no-op 兼容）

| 场景 | 预期 |
| --- | --- |
| 停留期内用户手关 tab | 到点后 `close_agent_session` 找不到 entry，no-op |
| 面板/窗口销毁时有待触发定时器 | receiver 语义自动取消，不炸 |
| 设置调为 0 | 等价补丁前行为：release 即关 |
| 用户手改 settings.json 越界/非数值 | load_settings 钳制/回默认，不炸 |
| AI 连续短命令 | 各 tab 结束后停留 N 秒，标签栏短暂堆叠后逐个到点消失（已确认的代价） |

### 验证

- offscreen 冒烟扩至 17 项 ×3 连跑全过：「release 后 tab 仍在 → 停留到点自动消失」「长命令（>4s）结束后同样停留满再关」（用户实机场景回归）「set_close_delay_seconds(0) 即时生效立即关」等，kill+release 用例适配停留时序
- 设置层单测：`load_settings` 值域防御 7 例全过（正常值/越界钳制/非 int/bool/缺键回默认/旧键丢弃）；设置中心终端页 offscreen 装配实测——reload 回读 4 且不触发写盘（`_reloading` 防回环有效）、用户调 8 秒即时下发 ctx
- 真机复核：待用户实机确认倒计时类长命令结束后停留 4 秒、设置调整即时生效
