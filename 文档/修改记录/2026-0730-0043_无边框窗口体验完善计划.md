> **状态**：阶段一/二已实施（2026-07-30）；阶段三经实机验证**无实施必要**，已拆出归档至 work plans/2026-0730-0127_无边框窗口阶段三增强项结论.md
> **范围**：`gui/title_bar.py`、`gui/main_window.py`、新增 `gui/window_resize.py`、`gui/theme.py`（title_bar 令牌包）、`assets/themes/base.qss`（未改动——D1 落 B 方案后无需动模板）
> **时间**：2026-07-30 00:43（设计，UTC+8）
> **优先级**：高（T1/T2 为基本功能缺失修复）
> **前置**：work plans/2026-0730-0007_自定义标题栏增量实施计划.md（已实施）

# 无边框窗口体验完善计划（标题栏配色 + 边缘缩放 + 增强项）

## 1. 目标

修复 `FramelessWindowHint` 无边框化后暴露的两个基本功能缺陷，并规划后续增强：

1. **标题栏配色割裂**：当前硬编码深色（`#252526`/`#cccccc`），与四个亮色主题视觉
   断裂 → 收编主题令牌体系，随主题切换联动。
2. **窗口边缘无法缩放**：无边框后原生缩放热区消失，鼠标移到边缘显示文本输入符
   （I-beam），用户无法拉大拉小窗口 → 自绘八向边缘缩放热区。
3. 其余增强项（窗口阴影、贴边吸附、标题栏右键系统菜单等）原排入阶段三，
   后经实机验证**无实施必要**（E1/E2 已实现，E3/E4/E5 无价值），已拆出归档至
   work plans/2026-0730-0127_无边框窗口阶段三增强项结论.md。

**零回归**：拖拽移动、双击最大化、三按钮、菜单栏定高截断、窗口几何持久化行为不变。

## 2. 现状摸底（设计前已核实）

| 事实 | 影响 |
|:---|:---|
| `gui/title_bar.py` `_BAR_BG/_BAR_TEXT/_BTN_HOVER_BG/_CLOSE_HOVER_BG` 四色硬编码，`TITLE_BAR_QSS` 模块级常量内联 | 配色改造点单一集中；收编令牌只需改常量来源 + 增加主题切换重刷入口 |
| 主题令牌集中于 `gui/theme.py` `THEME_PALETTES`（`window_bg/sidebar_bg/card_bg/border/accent/muted_text/button_hover_bg` 等），`apply_theme(app)` 经 `base.qss` 模板替换全局应用 | 标题栏收编有先例：`SettingsDialog.apply_theme` 按 `muted_text/border` 令牌重刷（`settings_dialog.py` §13–20 注释）；新增 `title_bar` 令牌包（每主题四色）或直接复用现有令牌，二选一（决策点 D1） |
| `MainWindow.apply_theme` 链（`main_window.py:371–379`）已逐个面板转发主题切换 | TitleBar 挂入该链即可联动，无需新信号 |
| 无边框化仅 `setWindowFlags(... \| FramelessWindowHint)` 一行；窗口几何持久化走 `WindowStateStore`（closeEvent 单一入口） | 手动缩放兜底改的是 `geometry()`，持久化语义不变；但须设 `minimumSize` 防缩坏三栏布局 |
| 终端组件注释（`panels/terminal/widget.py:280`）：未开 mouseTracking | 边缘热区需要 hover 光标反馈，须在**窗口级**开 `mouseTracking` 并给子控件树装事件过滤器（子控件各自设光标会盖掉热区光标——这正是当前边缘显示 I-beam 的原因） |
| 拖拽移动已实现 `QWindow.startSystemMove()` 优先 + 手动兜底双路径 | 缩放沿用同一选型：`startSystemResize(edges)` 优先（Qt6 原生 API，Wayland xdg-toplevel / X11 均由 WM 接管，流畅无闪烁），返回 False 时手动 `setGeometry` 兜底 |
| 最大化态拖拽还原已实现（`_restore_for_drag` 按横向比例跟随） | 最大化态下边缘缩放应禁用（与原生窗口行为一致） |

## 3. 方案选型

### 3.1 标题栏配色收编（决策点 D1）

| 方案 | 结论 |
|:---|:---|
| A. 复用现有令牌：底色 `sidebar_bg`、文字 `text`、hover `button_hover_bg`、关闭 hover 沿用 `#c75450` | ✅ 采纳（暂定）：零新增令牌，四主题即时生效；关闭按钮红为跨主题通用警示色，保留 |
| B. 每主题新增 `title_bar` 令牌包（bg/text/hover/close_hover 四键） | 备选：若 A 目测某主题观感不佳（如 `sidebar_bg` 与内容区无区分度），升级为 B，调色板加键即切换 |
| C. 保持深色不变 | ❌ 否决：用户明确反馈割裂 |

**实施要点**：`TITLE_BAR_QSS` 从模块级常量改为按调色板生成的函数；`TitleBar.apply_theme(theme)`
挂入 `MainWindow.apply_theme` 转发链（先例：`viewer_panel/terminal_panel/...` 同款签名）。

### 3.2 边缘缩放热区（核心决策）

| 方案 | 结论 |
|:---|:---|
| A. `QSizeGrip` 塞状态栏角落 | ❌ 否决：仅右下角单向，且与状态栏定高体系冲突 |
| B. 窗口级事件过滤器 + 八向热区判定，`startSystemResize(edges)` 优先 | ✅ 采纳：全边全角可用，WM 接管流畅，与拖拽移动（`startSystemMove`）技术路线对称 |
| C. 第三方库（如 FramelessWindow 移植） | ❌ 否决：引入重依赖，与现有 TitleBar 架构不匹配 |

**热区设计**：

- 热区厚度 `RESIZE_MARGIN = 6px`（窗口内缩判定；不向外扩——无边框窗口外扩需透明
  像素边距，侵入布局边距体系，本期不做）。
- 八向判定：`左/右/上/下` + 四角，映射 `Qt.Edge` 标志位组合；光标对应
  `SizeHorCursor / SizeVerCursor / SizeFDiagCursor / SizeBDiagCursor`。
- 事件路径：`MainWindow` 开 `WA_MouseTracking`，`QApplication` 级 `eventFilter`
  （或递归给子控件装过滤器——决策点 D2，实施时以「光标不被子控件 I-beam 覆盖」
  为验收标准择简者）拦截 `MouseMove/MouseButtonPress`：
  - 光标进入热区（未按下）→ `setCursor` 对应缩放光标；离开 → `unsetCursor`。
  - 热区内左键按下 → `windowHandle().startSystemResize(edges)`，返回 False 时
    记录起始几何与全局坐标，手动 `setGeometry` 兜底（MouseMove 驱动，释放结束）。
- 守卫：`isMaximized()` 或 `isFullScreen()` 时热区整体禁用；`minimumSize`
  建议设为三栏最小可布局值（实测后定，初估 900×500——决策点 D3）。

### 3.3 增强项（阶段三——已拆出归档）

阶段三增强项（E1 窗口阴影 + 微圆角、E2 贴边吸附、E3 标题栏右键系统菜单、
E4 拖拽/缩放中 ESC 取消、E5 窗口标题追加 Git 分支名/脏标记）已拆出至
work plans/2026-0730-0127_无边框窗口阶段三增强项结论.md。

**实机验证结论：阶段三整体无实施必要**——E1/E2 已实现（E2 如设计预判由 WM
自带 tiling 免费获得），E3/E4/E5 经评估无价值，本计划范围内不再跟踪。

## 4. 任务分解

### 阶段一：标题栏配色收编（P0，改动集中）

| # | 任务 | 改动点 |
|:--|:---|:---|
| T1 | `gui/theme.py`：确认 D1 选型（目测 A 方案四主题效果；不佳则落 B 方案加令牌键） | 调色板 ±4 键 |
| T2 | `gui/title_bar.py`：`TITLE_BAR_QSS` 常量 → `build_qss(palette) -> str` 函数；四硬编码色改读令牌；新增 `apply_theme(theme)` 重刷入口；docstring 更新（撤掉"固定深色"表述，标注收编决策） | 约 ±25 行 |
| T3 | `gui/main_window.py` `apply_theme` 转发链挂 `self.title_bar.apply_theme(theme)`；`__init__` 按启动主题初始化 | +2 行 |
| T4 | 四主题逐一切换目测：标题栏与菜单栏/内容区衔接、三按钮 hover、关闭 hover | 走查 |

### 阶段二：边缘缩放热区（P0，新组件 + 窗口集成）

| # | 任务 | 改动点 |
|:--|:---|:---|
| T5 | 新建 `gui/window_resize.py`：`EdgeResizeController(QObject)`——事件过滤器 + 热区判定 + 光标管理 + `startSystemResize` 优先/手动 `setGeometry` 兜底双路径；最大化/全屏守卫 | 纯新增约 120 行 |
| T6 | `gui/main_window.py`：`__init__` 实例化并安装控制器；设 `minimumSize`（D3 实测值）；`WA_MouseTracking` 开启范围按 D2 定案 | 约 +6 行 |
| T7 | 手动兜底路径单测冒烟：八向各拖一次，几何变化方向正确、最小尺寸约束生效、释放后状态干净 | 走查 |
| T8 | `startSystemResize` 路径实测（本机 WM）：光标形状、缩放流畅度、与拖拽移动互不干扰 | 走查 |

### 阶段三：增强项（已关闭）

E1–E5 已拆出归档：经实机验证阶段三**无实施必要**，详见
work plans/2026-0730-0127_无边框窗口阶段三增强项结论.md。

## 5. 回归走查清单（验收标准）

| # | 项 | 预期 |
|:--|:---|:---|
| R1 | 四主题切换 | 标题栏即时联动，无残留旧色；关闭 hover 红保持 |
| R2 | 八向缩放 | 光标形状正确（↔↕↘↙），不被子控件 I-beam 覆盖；拖动方向与几何变化一致 |
| R3 | 最小尺寸 | 缩到 `minimumSize` 截止，三栏布局不塌 |
| R4 | 最大化态 | 热区禁用；双击标题栏还原后热区恢复 |
| R5 | 既有交互 | 拖拽移动、最大化态拖拽还原跟随、三按钮、菜单栏行为不变 |
| R6 | 几何持久化 | 手动缩放后关闭重开，尺寸恢复正确（`WindowStateStore` 链路） |
| R7 | 多显示器 | 跨屏拖动后缩放热区判定不漂移（走查项） |
| R8 | 自动截图 | `uv run main.py --auto-screenshot --screenshot-on-start` 出图正常 |

## 6. 决策点留档

| # | 问题 | 选项 | 实施时定案 |
|:--|:---|:---|:---|
| D1 | 标题栏取色 | A 复用现有令牌 / B 新增 `title_bar` 令牌包 | **已定 B**：A 的 `sidebar_bg` 与 `window_bg` 色差 <2%（云白 #fafafb vs #ffffff），标题栏与菜单栏/内容区无区分度；B 落为每主题 `title_bar` 包四键（bg/text/hover/close_hover），`PACK_KEYS` 同步 |
| D2 | 热区事件拦截方式 | QApplication 级过滤器 / 递归子控件过滤器 | **已定 QApplication 级过滤器**（`EdgeResizeController` 构造时 `app.installEventFilter`）：免追踪动态子控件；配合 MainWindow 开 `WA_MouseTracking`，未开 tracking 的子控件 hover 移动事件沿父链上冒至窗口，过滤器收全 |
| D3 | `minimumSize` 数值 | 实测三栏最小可布局值（初估 900×500） | **已定 900×500**（左栏 ChatTabs 静态下限 315 + 右栏 FileExplorer 240 + 中栏 + 边距/手柄经验值）；手动兜底路径越界拖钳制已冒烟验证 |
| D4 | 关闭按钮 hover 红 `#c75450` 是否随主题微调 | 保持 / 入令牌 | **已定入令牌但四主题同值 #c75450**（跨主题通用警示红，入包便于后续微调） |

## 7. 实施记录（2026-07-30）

- **T1–T4（阶段一）**：`gui/theme.py` 新增 `TitleBarPack` TypedDict + 四主题
  `title_bar` 令牌包；`gui/title_bar.py` `TITLE_BAR_QSS` 常量 → `build_qss(pack)`
  函数，新增 `apply_theme(theme)`（构造期经 `load_settings` 自检启动主题，
  切换走 `MainWindow.switch_theme` 转发链——同 settings_dialog 先例）；
  `check_theme_tokens.py` 双向比对通过；四主题 offscreen 切换截图走查
  （`.tmp/shoot_themes.py`）确认即时联动无残留旧色。
- **T5–T8（阶段二）**：新增 `gui/window_resize.py` `EdgeResizeController`
  （约 150 行）：八向热区 6px 内缩判定、`startSystemResize(edges)` 优先 /
  手动 `setGeometry` 兜底、最大化/全屏守卫、hover 光标覆盖/还原管理；
  `main_window.py` 一行安装 + `setMinimumSize(900, 500)`。
  冒烟测试 `.tmp/test_window_resize.py` 30 项全过（八向判定、光标映射、
  手动兜底八向几何、minimumSize 钳制、外来窗口事件不拦截）；
  `--auto-screenshot` 启动出图正常（R8）。
- **待实机走查**（offscreen 无法覆盖）：R2 光标形状不被子控件 I-beam 覆盖、
  R4 最大化态热区禁用/还原恢复、R5 既有交互回归、R6 几何持久化、R7 多显示器、
  T8 `startSystemResize` 路径实机流畅度（本机 WM 支持时走原生路径，
  手动兜底已被冒烟覆盖）。
- **阶段三结论（2026-07-30 实机验证）**：E1（窗口阴影已实现，微圆角经评估
  不需要）、E2（贴边吸附，WM 自带 tiling 免费获得）已落地；E3/E4/E5 经评估
  无必要。阶段三关闭，详见 work plans/2026-0730-0127_无边框窗口阶段三增强项结论.md。
