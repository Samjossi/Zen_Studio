> **状态**：代码改动经 offscreen 验证后按用户决定整体回滚未入库（提交 `3145857` 已注明）；修复路线由 `2026-0820-1642_主题切换重启生效化与调色板纳管实施计划.md` 取代（重启生效 + QPalette 纳管）；本文档 §2 根因调查结论保留备查
> **范围**：`assets/themes/base.qss`（文本类控件 color 令牌化）；`gui/panels/chat/`（存量块重刷机制、模块级色值捕获清除）；`gui/panels/viewer/toggle_switch.py`、`gui/panels/chat/permission_dialog.py`、`gui/settings_dialog.py`、`gui/git_graph_view.py` 零散漏网点
> **时间**：2026-08-20 14:24（设计）(UTC+8)
> **优先级**：高（深色主题真机可用性阻断项）
> **关联**：`work plans/2026-0820-1115_双深色主题新增实施计划.md`（深色主题引入后暴露的欠账）；根因调查为 2026-08-20 会话全库色值统计（thorough 探查 + 逐像素实证）

# 文本色纳管与存量卡片重刷修复计划

## 1. 目标

1. **文本色令牌化收口**：全部文本编辑类控件（QTextBrowser/QTextEdit/QPlainTextEdit/QLineEdit 及其子类实例）文字色纳入 `text` 令牌管理，深浅主题下均正确着色。
2. **存量块重刷**：聊天卡片轨已存在的块（气泡/工具卡/思维链/diff 卡等）在主题切换时同步换色，消灭「切换后白底黑字」。
3. **零散漏网清零**：模块级 `CHAT_PACK` 捕获、跨主题固定 `WARNING_COLOR`、开关轨道硬编码色、输入框选区带无刷新、图委托默认色残留，逐项纳管。
4. **亮色四主题视觉零回归**：渲染 diff 仅允许白名单内差异（新增 color 声明行），offscreen 截图对比确认。

## 2. 根因（已实证，非推测）

`base.qss:18` 注释论断「qss 的 color/selection 沿控件树继承」**不成立**（初稿仅断言文本编辑类，实施期探针复查进一步证伪 QLabel 分支）。offscreen 逐像素探针实测：

- `QMainWindow { color: #eeeeee }` 下 `QLabel` 文字像素仍恒为 `(0,0,0)` ❌ 不继承——初稿此处记为「✅ 继承」系误判，实施期以最小样式表对照探针证伪（palette Text 角色恒黑）；菜单栏 QMenuBar 同病
- 同条件 `QTextBrowser`/`QTextEdit`/`QPlainTextEdit`/`QLineEdit` 文字像素恒为 `(0,0,0)` ❌ 不继承——落到 widget 调色板 Text 角色，而主题切换只设样式表不碰 palette（`gui/settings_dialog.py:15` 注释自证）
- 仅当 QSS 规则**显式命中该类**并写 `color` 才生效（`markdown_view.py:214` 的 document 默认样式表是既有正确范式）

> 订正说明：QLabel/QMenuBar 文本色残留属本计划外欠账（深底下黑字，见 `.tmp/theme_walkthrough/graphite_main.png` 菜单栏），本计划未修，`base.qss` 顶部注释已按订正后论断改写；建议后续单独立项处置。

亮色四主题正文色 ≈ 黑，缺陷潜伏；深色主题使其系统性暴露。次要根因：聊天卡片族颜色在构造期冻结进内联 QSS/HTML，`transcript.py:193` 注释自认「主题切换仅影响此后新建的块」。

## 3. 牵连面清单（2026-08-20 统计结论，按修复分组）

### A 类：文本恒黑（base.qss 规则补 `color: $text` 覆盖）

| 控件源 | 波及 |
|:---|:---|
| `base.qss` `QLineEdit, QTextEdit` 规则（第 122 行段）无 color | 全局输入框、查找条（`find_bar.py:46`）、设置中心黑名单框（`settings_dialog.py:282`）、旧轨 ChatOutput（QTextBrowser 子类）、审批详情框（`permission_dialog.py:74`） |
| `base.qss` `QPlainTextEdit` 规则（第 131 行段）无 color | 代码查看器未高亮文字、md 源码模式、`CardBodyText`（bash 卡/MCP 入参/提问卡/todo 摘要） |
| `CardBodyHtml`（QTextBrowser 子类） | 新轨 AI 正文流、系统块、用户气泡正文、MCP 出参、diff 未着色行——经 QTextEdit 规则覆盖 |
| `base.qss` `QStatusBar` 规则（第 456 行段）无 color | 状态栏消息（🟡 疑似，实施后实测确认） |

> 级联原理：控件级内联 QSS（`#CardBodyText { background: transparent; }` 等）未设 color，app 级类选择器规则的 color 正常生效；类选择器匹配子类（QTextBrowser←QTextEdit 等），一处模板改动全覆盖。

### B 类：存量块切换不重刷（新增重刷机制）

| 位置 | 冻结内容 |
|:---|:---|
| `transcript.py:165` UserBubbleBlock | 气泡底 QSS + 链接化 HTML（link_fg） |
| `cards.py:404` 工具卡外壳 + 约 20 处标题/状态 label | `#ChatToolCard` 底、tool_fg/tool_error_fg 等 |
| `cards.py:699-748` 思维链/diff 卡 | `<font color>`/`<span style>` 内联色（reasoning_fg/diff_add_fg/diff_del_fg） |
| `output.py:266-503` 旧轨 ChatOutput | 同款内联 HTML 色（冻结回退轨，见决策点 D2） |

### C 类：零散漏网（逐项纳管）

| # | 位置 | 问题 | 修法 |
|:---:|:---|:---|:---|
| C1 | `permission_dialog.py:20` | 模块级 `from gui.theme import CHAT_PACK` 永久捕获亮套；对话框不在切换转发链 | 构造时按当前主题动态取 `get_theme_palette(...)["chat"]` |
| C2 | `permission_dialog.py:72`、`settings_dialog.py:92` | 危险警示行跨主题固定 `WARNING_COLOR #c0392b`（深底对比度约 2.5:1 不足） | 改取当前主题 chat 包 `tool_error_fg`（亮 #c0392b / 深 #e06c75，语义一致零新增键） |
| C3 | `toggle_switch.py:34-36` | Markdown 开关轨道硬编码 `#0765d4`（midnight accent 为 #2f81f7，不一致） | 色值改构造注入，`viewer/panel.py:501` apply_theme 传 `palette["accent"]` 重刷 |
| C4 | `input.py:122` | ChatInput 选区带色构造注入后无刷新 | `ChatPanel.apply_theme`（`panel.py:708`）补 `input` 带色更新 |
| C5 | `git_graph_view.py:132,207` | 委托构造期硬编码默认色（随即被 apply_theme 覆盖，瞬时残留） | 默认值改为构造时按当前主题取包，消残留 |

## 4. 目标状态设计

### 4.1 A 类：模板补色（base.qss 三处规则各加一行）

```css
/* assets/themes/base.qss */
QLineEdit,
QTextEdit {
    background-color: $input_bg;
    color: $text;            /* 新增：文本色令牌化（根因见计划 §2） */
    border: 1px solid $border;
    ...
}
QPlainTextEdit {
    background-color: transparent;
    color: $text;            /* 新增 */
    ...
}
QStatusBar {
    background-color: transparent;
    color: $text;            /* 新增 */
}
```

同时**改写 base.qss 第 18 行段的错误注释**：「沿控件树继承」仅对 QLabel 等非编辑类成立，文本编辑类必须显式命中——防止后人再按错误论断加控件。

### 4.2 B 类：存量块重刷机制

原则：**CardColors 袋共享引用语义不变**，每块类保留袋引用 + 原始数据，新增 `refresh_colors()`；`ChatTranscriptView.apply_theme` 更新袋后遍历已建块逐个调用。

```python
# gui/panels/chat/transcript.py（示意）
def apply_theme(self, user_bubble_bg, tool_output_bg, link_fg):
    self._colors.user_bubble_bg = user_bubble_bg   # 既有 setter 逻辑保留
    ...
    for block in self._iter_blocks():              # 新增：存量块重刷
        block.refresh_colors()

# UserBubbleBlock：构造时留存 content/images，refresh_colors 重建内联 QSS
# 与链接化 HTML（颜色全部从袋重取）
# ToolCard：refresh_colors 重建外壳 QSS + 标题/状态 label 样式
# 思维链/diff 卡：构造时留存原始文本/hunk 数据，refresh_colors 重生成 HTML
```

- 逐块实施时如某卡类原始数据未留存，先补簿记再重渲染，**不做近似刷新**（防色值与数据不一致的中间态）。
- 决策点 D2：**旧轨 ChatOutput 存量块不追溯**。旧轨已是冻结回退通道（0645 计划 D2-A），其正文色经 A 类 `QTextEdit` 规则自动修复，存量 HTML 内联色（气泡底/思维链灰等）维持「仅影响此后追加的块」语义。新轨卡片为默认轨，重刷投入集中在该处。

### 4.3 C 类：按 §3 表格逐行实施，无新增主题键

## 5. 实施步骤

| # | 步骤 | 产出 |
|:---:|:---|:---|
| 1 | base.qss 三处规则补 `color: $text` + 第 18 行段注释纠错 | base.qss |
| 2 | 亮色回归第一门：渲染 diff vs HEAD 仅含白名单差异（3 行新增 color）；四亮主题 offscreen 截图对比文字色 | diff 报告 + 截图 |
| 3 | A 类实证：探针逐像素断言六主题下 BodyHtml/BodyText/CodeViewer/输入框/查找条/状态栏文字色 == 各主题 text 令牌值 | 探针脚本通过 |
| 4 | B 类：UserBubbleBlock/ToolCard/思维链卡/diff 卡补原始数据簿记 + `refresh_colors()`；transcript.apply_theme 遍历重刷 | cards.py、transcript.py |
| 5 | B 类实证：复现用户场景——亮主题下建卡 → 运行时切深色 → 旧卡逐像素换色断言 | 探针脚本通过 |
| 6 | C1-C5 逐项实施 | 对应文件 |
| 7 | `.tmp/check_theme_tokens.py` + `scripts/check_qss_selector_guard.py` 复跑 | 通过 |
| 8 | 深色走查脚本重跑（`.tmp/verify_1115_walkthrough.py`）+ 运行时切换场景截图 | 截图存档 |
| 9 | 计划文档回填 + 1115 计划状态联动更新 | 文档 |
| 10 | 真机人工终验 | 用户确认 |
| 11 | 单提交（与 1115 改动可分别 revert） | 提交 |

## 6. 验证方案（检查点）

| # | 检查点 | 预期 |
|:---:|:---|:---|
| 1 | 亮色渲染 diff | 仅白名单（新增 color 行），无其他差异 |
| 2 | 六主题文本色逐像素断言 | 全部 == 各自 text 令牌 |
| 3 | 运行时切换存量块重刷 | 旧卡底色/文字/内联 HTML 色全部换新包值 |
| 4 | 亮色四主题视觉对比截图 | 与实施前无可见差异 |
| 5 | 令牌校验 + QSS 守卫 | 通过 |
| 6 | rg 残留核对 | 无新增模块级色值捕获；`WARNING_COLOR` 仅剩语义内引用 |

## 7. 风险与回滚

| 风险 | 等级 | 应对 |
|:---|:---:|:---|
| base.qss 加 color 触发某控件意外变色 | 🟡 | 检查点 1/4 双门（diff 白名单 + 截图对比）；文本编辑类亮色 text ≈ 原默认黑，理论无感 |
| B 类逐块簿记引入状态不一致 | 🟡 | 每卡类先补原始数据簿记再重渲染；检查点 3 逐像素断言 |
| 控件级内联 QSS 改动触发 0811 级联菜单问题 | 🟡 | 不改无选择器写法；QSS 守卫脚本复跑兜底 |
| 旧轨（D2）用户切回时存量卡仍滞留旧色 | 🟢 | 冻结回退通道语义，计划内明示接受 |
| midnight 开关轨道色变更后观感 | 🟢 | 走查截图确认 |

回滚：全部改动收敛为单提交，`git revert` 恢复。

## 8. 实施结果回填（2026-08-20）

A/B/C 三类全部落地，五拍闭环取证齐备（修前探针 → 截图亲验 → 修复 → 同一脚本复测 → 回归补强）：

| 检查点 | 结果 | 证据 |
|:---:|:---|:---|
| 1 亮色渲染 diff | ✅ 六主题渲染 vs HEAD 快照逐行 diff，每主题恰好新增 3 行 `color: <该主题 text 值>;`，无其余差异（注释差异剥离后比对） | `.tmp/verify_1424_render_diff.py` |
| 2 六主题文本色逐像素断言 | ✅ 六主题 × QLineEdit/QTextEdit/QPlainTextEdit/BodyHtml/BodyText/QStatusBar 全部命中各自 text 令牌，纯黑像素 0；QLabel 对照组为信息项（ℹ️，印证 §2 订正） | `.tmp/probe_text_colors.py`、`.tmp/1424_probe/text_{theme}.png`（已逐张亲验） |
| 3 运行时切换存量块重刷 | ✅ 亮套建卡 → 切石墨：气泡底 12821px / 卡壳 81797px 换深色，思维链/diff 红绿/失败红/工具灰全换深色包值，亮套气泡底残留 13px（抗锯齿噪声，阈值 ≤20 放行） | `.tmp/probe_block_refresh.py`、`.tmp/1424_probe/blocks_dark_switched.png`（已亲验） |
| 4 亮色视觉对比 | ✅ 检查点 1 白名单 diff 等价保证（亮色 text ≈ 原默认黑，无可见差异） | 同检查点 1 |
| 5 令牌校验 + QSS 守卫 | ✅ `6 主题 × 26 令牌双向一致，1259 反模式零命中`；`chat/ 无无选择器 background 声明` | `.tmp/check_theme_tokens.py`、`scripts/check_qss_selector_guard.py` |
| 6 残留核对 | ✅ `WARNING_COLOR` 仅剩 `gui/theme.py` 调色板定义内语义引用；无新增模块级色值捕获（`scripts/` 截图工具内引用为离线构造用途，非运行时切换路径） | grep 全库 |
| 附 C 类专项 | ✅ C1/C2 审批/设置警示行随主题取 `tool_error_fg`、C3 开关轨道注入 + 切换重刷、C4 输入框选区带色 `#58a6ff`、C5 图委托默认色取自调色板 | `.tmp/probe_c_class.py` |
| 附 1115 联动回归 | ✅ 六主题循环切换持久化/勾选态正确；石墨/深夜走查三图落盘并亲验（聊天区正文、气泡、状态栏均浅色可读） | `.tmp/verify_1115_switch.py`、`.tmp/verify_1115_walkthrough.py`、`.tmp/theme_walkthrough/` |

计划外发现（未修，建议后续单独立项）：QLabel/QMenuBar 文本色同样不沿控件树继承 qss color（§2 已订正），深底下菜单栏等仍为黑字，见 `.tmp/theme_walkthrough/graphite_main.png` 菜单栏。

待办：真机人工终验（第二级闭环，待用户确认）；确认后单提交。

---

*设计：2026-08-20 14:24 (UTC+8) | 实施：2026-08-20（检查点 1-6 全过，待真机人工终验）*
