> **状态**：实施完成（代码在工作区），offscreen 验证已过，真机视觉闭环待补
> **范围**：聊天区 AI 对话栏选区高亮（`gui/panels/chat/output.py`）；公共辅助 `gui/selection_band.py`（新建）；Markdown 预览页重构接入（`gui/panels/viewer/markdown_view.py`）
> **时间**：2026-07-31 21:45 (UTC+8)（立项 + 实施）
> **优先级**：高

# 聊天区选区高亮偏下修复计划（方案 A 通用化）

## 1. 问题与定位

2055 计划（`2026-0731-2055_Markdown预览选区高亮偏下诊断与修复计划.md`）
修复了 **Markdown 预览页**选区高亮偏下（✅ 真机已确认修好），但当时把
**聊天区 AI 对话栏**的同款问题登记为 T4「另行立计划」未实施——
用户在 AI 回复文字上滑选时，高亮带依然偏下。本文档即 T4 的立项承接。

根因与 2055 §2 完全相同：聊天区 ChatOutput 同为 QTextBrowser + 思源黑体，
原生选区带 = QTextLine 整行高（leading 6.5px 全部垫底），
带上缘留白 2px/下缘 7px 视觉偏下。修复路径 = 方案 A 通用化。

## 2. 实施方案（与 2055 方案 A 同构，抽公共辅助）

| # | 改动 | 文件 |
|---|---|---|
| T1 | 新建公共辅助模块：SUPPRESSION_QSS（抑原生带+保正文色）、`selection_rects()`（逐行视口矩形，墨盒±3px 对称留白）、`paint_selection_band()`（半透明圆角带叠绘） | `gui/selection_band.py`（新建） |
| T2 | 聊天区接入：构造器 setStyleSheet(SUPPRESSION_QSS)；新增 paintEvent 基类绘制后自绘；带色复用 link_fg（timeline_read_fg 同源蓝，守不新增主题键纪律） | `gui/panels/chat/output.py` |
| T3 | Markdown 预览页重构：删除私有 _selection_rects/_line_x/常量，改调公共辅助（行为零变化） | `gui/panels/viewer/markdown_view.py` |

## 3. 验证记录

### 3.1 offscreen 双帧差分（`.temp/verify_selection_band.py`，已过 ✅）

| 场景 | 选区带 | 字形墨 | 上留白 | 下留白 | 带中心偏差 |
|---|---|---|---|---|---|
| MarkdownView / 无应用 qss | 高 19px | 高 13px | +3px | +3px | +0.0px ✅ |
| MarkdownView / 带应用 qss（真机环境） | 高 19px | 高 13px | +3px | +3px | +0.0px ✅ |
| **ChatOutput / 带应用 qss（真机环境，mock 用户气泡+AI 流式回复）** | 高 19px | 高 13px | **+3px** | **+3px** | **+0.0px** ✅ |

- 复现真机环境的关键：探针挂应用级 qss
  `QMainWindow, QDialog { selection-background-color: accent }`
  （`assets/themes/base.qss` 同款继承规则），证实控件级 SUPPRESSION_QSS
  在真机级联下依然生效；
- 跨段选区分段断言三场景全过。

### 3.2 存量回归（已过 ✅）

- `.temp/smoke_chat_render.py` 20 断言全绿；
- `.temp/smoke_chat_render_l2.py` 25 断言全绿；
- 三改动文件 `py_compile` 全过。

### 3.3 真机视觉闭环（⏳ 进行中，被意外打断）

- 路径：源码启动 IDE（`--auto-screenshot --screenshot-interval 1`）
  + XTEST 驱动（`.temp/xdrive.py` 点击/拖拽/组合键）
  + 剪贴板挂料注入中文消息（`.temp/xpaste.py`，规避 fcitx 预编辑
  干扰 XTEST 逐键输入的坑——实证：直打 ASCII 会被 IME 转成
  中文预编辑文本）；
- 进度：Markdown 预览真机修复已由用户确认；聊天区真机驱动至
  「清空输入框→粘贴消息」步骤时 IDE 进程意外退出（原因未查清，
  疑似 XTEST 组合键落于错误焦点窗口触发关闭），闭环未完成；
- 收尾动作：重启 IDE → 粘贴发消息 → 等 AI 回复 → XTEST 拖拽选中
  回复文字 → 截图差分度量（脚本现成），或直接请用户在日常对话中
  滑选确认。

## 4. 合规与现场记录

- 主题键零新增（带色复用 accent / link_fg 令牌）；
- 临时产物（不入库）：`.temp/verify_selection_band.py`、
  `.temp/xdrive.py`、`.temp/xpaste.py`；
- 临时依赖 python-xlib（真机驱动用）已装入 venv，闭环收尾后按需卸载；
- 2055 计划 §6 T4 行已指向本文档。

## 5. 待办（真机闭环收尾）

| # | 任务 | 状态 |
|---|---|---|
| F1 | 重启 IDE，完成聊天区真机拖拽选区截图差分（或用户自然使用确认） | ⏳ |
| F2 | 真机确认后卸载 python-xlib、清理 `.tmp/` 截图 | ⏳ |
| F3 | 本计划状态改「已验证」 | ⏳ |
