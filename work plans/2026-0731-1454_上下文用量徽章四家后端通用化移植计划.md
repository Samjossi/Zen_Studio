> **状态**：初步草案（🔴 未实施；D1–D4 待用户拍板、E1–E3 待只读实证后方可转正）
> **范围**：`llm/base.py`（改：UsageStats 扩 source 字段）、`llm/context_limits.py`（新：模型上下文上限表）、`llm/providers/reasonix_acp.py`（改：transcript 解析）、`llm/providers/kimi_acp.py`（改：估算或会话文件，依 D1）、`gui/panels/chat/panel.py`（改：口径标注）
> **时间**：2026-07-31 14:54（创建，UTC+8）
> **优先级**：中
> **关联**：`work plans/2026-0731-1412_上下文Token用量百分比显示移植计划.md`（已实施，本计划是其通用化续篇——1412 解决「有推送的后端」，本计划解决「不推送的后端」）

# 上下文用量徽章四家后端通用化移植计划

> ⚠️ **本文档为初步文档**：官网机制查证与四家协议实证已闭环，但
> 关键决策（§5 之 D1–D4）与三项只读实证（§4 之 E1–E3）未完成。
> 拍板后修订转正，再进入实施。

## 1. 背景与动机

1412 计划落地后实测发现：`usage_update` 是 **kilocode / opencode 两家
专有**的 ACP 推送，**kimi / reasonix 的 ACP 出口均不转发用量数据**
（1412 文档 T5 实测矩阵）。用户诉求：百分比徽章对四家后端通用。

## 2. 官网机制查证（2026-07-31 14:54）

**核心问题：其他 IDE/官方前端用 kimi/reasonix 时怎么知道上下文百分比？**

答案：**没有魔法渠道——是 agent 自己内部知道，只是 ACP 出口没接。**

- **Kimi Code CLI**：官方帮助页「Sessions & context」明确写道
  「The status bar at the bottom of the CLI displays the current context
  utilization in real time」（CLI 底部状态栏实时显示上下文利用率，用量
  高时提示 /compact）。数据源是 agent 与模型 API 的直接交互——OpenAI
  兼容协议每次响应必带 `usage` 字段，agent 内部天然持有；kimi 只是
  **未将其转发到 ACP**（本机实测：ACP update 流无 usage_update，
  prompt 响应帧仅 `stopReason`）。
  另据官方「Getting started」：本地数据存于 `~/.kimi-code/`（config、
  **session records**、logs）——会话落盘记录是潜在的协议外数据源。
- **Reasonix**：官网文档描述其 TUI 响应式底栏「按可用性显示 Git、
  缓存、**上下文**、压缩余量、任务和余额遥测」。Reasonix 直连 OpenAI
  兼容 provider（DeepSeek），用量同样来自 API 响应 `usage`（其主打
  前缀缓存，缓存命中 token 是内部核心指标）。其官方 ACP.md（219 行
  全文查证）**通篇无 usage 相关方法/通知**——确认 ACP 面无出口；
  但 prompt 响应携带 **`transcriptPath`**（指向官方持久化转录
  `~/.reasonix/sessions/*.jsonl`，属 ACP 会话生命周期正式产物，
  `session/load` 回放即依赖它），是本计划 L2 路径的依据。

**结论**：用量数据在四家 agent 内部都存在（模型 API 响应 `usage`），
kilocode/opencode 多做了一步把它包成 `usage_update` 推给 ACP 客户端；
kimi/reasonix 没做这一步。通用化 = IDE 侧为后两家找协议外数据出口
或接受估算降级。

## 3. 协议实证矩阵（1412 T5 + 本计划补测，本机真机各一轮）

| agent | `usage_update` 通知 | prompt 响应 `result.usage` | 协议外线索 |
|---|---|---|---|
| kilocode（7.4.17） | ✅ used/size/cost 同帧 | ✅ inputTokens/cachedReadTokens… | — |
| opencode（1.18.9） | ✅ 同帧 | ✅ inputTokens/thoughtTokens… | — |
| kimi | ❌ | ❌（仅 stopReason） | `~/.kimi-code/` 会话落盘记录（官网证实存在，内容未实证 → E2） |
| reasonix（1.17.21） | ❌ | ❌（stopReason + **transcriptPath**） | 持久化转录 jsonl（内容未实证 → E1） |

另实证：registry 模型条目是纯字符串（如 `kimi-code/k3-256k`），
**无上下文上限元数据**——1412「不自建上限表」的边界在通用化下
必须重议（见 §4-D 上限表）。

## 4. 方案：多级数据源降级链

徽章数据源从「唯一依赖 usage_update」改为按精度分级的降级链，
provider 层统一在轮次结束时产出 `UsageStats`，GUI 层架构不变：

```
L0  usage_update 推送（kilocode/opencode，现状已通）—— 最准，used/size 同帧
L1  prompt 响应 result.usage ÷ IDE侧上限表
    —— kilocode/opencode 已实证有；仅作 L0 冗余，不单独使用
L2  transcript 转录文件解析（reasonix 专有路径）—— 响应给 transcriptPath，
    读 jsonl 取最后一条 assistant 的 usage ÷ IDE侧上限表（依赖 E1）
L3  本地估算（kimi 兜底）—— 已发历史 tokenizer 粗估 ÷ 上限表（依 D1；
    偏差来源：agent 注入的 system prompt/工具定义 IDE 不可见）
```

### 4.1 UsageStats 扩来源字段

```python
@dataclass(frozen=True)
class UsageStats:
    used: int
    size: int
    cost: float | None
    source: Literal["push", "response", "transcript", "estimate"] = "push"
```

GUI 对非 `push` 来源标注口径（§5-D3）。

### 4.2 模型上下文上限表（新模块 `llm/context_limits.py`）

- 静态内置表：registry 模型别名字符串 → context tokens
  （`"kimi-code/k3-256k": 262144`、`"deepseek/deepseek-chat": 128000` …）；
- 数值来源：各家官方文档公开数据，与 kilocode/opencode 推送的
  size 实测值交叉校验（实测已见 262144 / 1000000 / 200000 三种）；
- 缺表项 → 该后端降级隐藏徽章，不臆造上限（§5-D4）。

### 4.3 待实证项（转正前先做的只读查证）

| # | 事项 | 决定什么 |
|---|---|---|
| E1 | 解析 reasonix transcriptPath 指向的 jsonl，确认是否含逐条 token usage | L2 是否成立（不成立则 reasonix 同 kimi 走 D1 路径） |
| E2 | 探查 `~/.kimi-code/` 会话落盘记录是否含用量数据 | kimi 走「读会话文件」还是「L3 本地估算」 |
| E3 | kilocode/opencode 模型目录命令是否吐 contextWindow 元数据 | 上限表能否半自动生成（否则全手写 28 个别名） |

⚠️ E1/E2 涉及读 agent 家目录文件，超出项目目录边界，需用户授权。

## 5. 待决策点（🔴 拍板前不定稿）

| # | 决策点 | 候选 | 初步倾向 |
|---|---|---|---|
| D1 | kimi 的口径 | A：L3 本地估算（chars/4 或轻量 tokenizer，UI 标注估算）；B：E2 有果则读 kimi 会话文件（精度更高）；C：保持隐藏等官方 | **B 优先、A 兜底**（E2 有数据走 B，无数据走 A；接受估算偏差 2~5 倍并如实标注） |
| D2 | reasonix 是否做 L2 transcript 解析 | A：做（E1 成立时精度接近真值）；B：与 kimi 同路径 | **A 若 E1 成立**（transcriptPath 是响应帧正式字段、官方生命周期产物；格式漂移用「解析失败降级」兜底） |
| D3 | 非 push 来源的 UI 标注 | A：`~12%` 波浪号前缀 + tooltip 注明口径（transcript/估算）；B：不标注 | **A**（诚实呈现精度等级；push 来源保持 `12%` 无修饰） |
| D4 | 上限表缺项的后端 | A：隐藏徽章；B：只显 token 数不显百分比 | **A**（与 1412 既定降级语义一致） |

## 6. 任务分解（拍板后排期）

| # | 任务 | 文件 | 依赖 |
|---|---|---|---|
| T1 | E1–E3 只读实证查证，结论补入本文档 | — | 用户授权家目录只读 |
| T2 | 模型上下文上限表模块 + 查询函数（缺项返回 None） | `llm/context_limits.py`（新） | E3 |
| T3 | `UsageStats` 扩 source 字段；provider 层降级链骨架（L0→L1 已有数据路径统一） | `llm/base.py`、`llm/providers/acp.py` | T2、D3 |
| T4 | reasonix transcript jsonl 解析器（L2；解析失败静默降级） | `llm/providers/reasonix_acp.py` | E1、D2、T3 |
| T5 | kimi 用量来源（D1：会话文件读取或本地估算器） | `llm/providers/kimi_acp.py` | E2、D1、T3 |
| T6 | GUI 口径标注（`~` 前缀 + tooltip 文案分级） | `gui/panels/chat/panel.py` | D3、T3 |
| T7 | 四家实测闭环 + offscreen 截图验证（含标注形态） | `.temp/` | T4–T6 |

## 7. 风险与边界

| 风险 | 评估 | 对策 |
|---|---|---|
| kimi 估算严重失真（agent 侧注入内容不可见） | 高（2~5 倍偏差可能） | UI 标注估算口径；tooltip 写明「不含 agent 侧系统开销」；E2 优先找真数据源 |
| reasonix transcript 格式随版本漂移 | 中 | 解析失败静默降级 L3/隐藏；格式字段防御性取值 |
| 上限表过期（新模型上架/窗口扩容） | 中 | 缺项隐藏不臆造；L0 推送的 size 实测值可回写校验表 |
| 读 agent 家目录文件的权限与路径漂移 | 低 | 路径从响应帧 transcriptPath / 官方文档默认目录取，失败即降级 |

**不做的事**：不给 kimi/reasonix 的 agent 侧提协议修改（不可控）；
不改 ACP 连接层；不引入重型 tokenizer 依赖（估算用轻量启发式）；
L1 response.usage 不作独立数据源（仅 kilocode/opencode 有，已被
L0 覆盖，留作未来校验）。
