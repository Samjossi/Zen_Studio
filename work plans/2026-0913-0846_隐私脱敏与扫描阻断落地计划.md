# 隐私脱敏与扫描阻断落地计划（协议 v2.0 存量接入）

> **状态**：部分已实施（2026-09-13：阶段 A/B 落地并双向验收通过；阶段 C 历史重写经评估放弃——泄漏仅为本机用户名与目录结构的指纹级信息，重写成本与收益不匹配，流程保留备查；阶段 D 托管侧加固待另行安排）
> **范围**：`.gitignore`、`scripts/privacy_scan.sh`（新建）、`scripts/check.sh`、`.githooks/pre-commit`（新建）、仓库级 git 配置、`tests/fixtures/reasonix_frames.jsonl`、git 历史（阶段 C）
> **时间**：2026-09-13 08:46（设计）(UTC+8)
> **优先级**：高

---

## 1. 背景

用户提供《隐私脱敏与扫描阻断通用协议 v2.0》（`协议和指南/隐私脱敏与扫描阻断通用协议_v2.0.md`，
该目录整体在 `.gitignore` 中，协议文本不入库），要求按协议为本存量项目建立扫描阻断钩子。

本计划按协议 §五（本地三层）+ §七（存量接入）适配落地。协议 §八 托管侧两层
（Forgejo 服务端 pre-receive、GitHub push protection）需 NAS 服务端与 GitHub 后台操作，
列入阶段 D，由用户决策后另行执行。

## 2. 现状核查结论（2026-09-13 已实测）

| 层 | 现状 |
|:---|:---|
| 身份 | 全历史提交身份均为 `Samjossi <144557423+Samjossi@users.noreply.github.com>`（GitHub noreply 匿名形态）✔；但 `user.name` 未做仓库级配置（继承全局），需按 §五-2 补齐 |
| .gitignore | 已覆盖 `AGENTS.local.md`、`.venv/`、`.temp/`、构建产物等；**缺** `*.local.md` 通配、`.env`/`.env.*`、密钥扩展名（`.pem/.key/.keystore/.jks/.p12`）、`*.log`、AI 工具目录（`.claude/`、`.aider*`、`.cursor/`、`*.code-workspace`）、`desktop.ini` |
| 存量泄漏 | 🔴 `tests/fixtures/reasonix_frames.jsonl` 含真实路径 `/home/<真实用户名>/...` 与 `~/.reasonix/...`，且已随提交 `a9554c0` 进入历史 |
| 占位符命中（安全） | `文档/修改记录/2026-0725-1234_...md` 的 `/home/xxx` 是协议认可的占位写法，扫描脚本按协议内置豁免规则放行 |
| 门禁 | `scripts/check.sh` 现有 ruff + pytest，无隐私扫描段；无 `scripts/privacy_scan.sh`；无 `.githooks/`；`core.hooksPath` 未配置 |
| AGENTS.md | 已有"提交前必跑 `scripts/check.sh`"条款 ✔（扫描并入 check.sh 后自动对 AI 生效） |

## 3. 阶段划分

### 阶段 A：本地三层卡口（防未来，无破坏性）

| # | 任务 | 依据 |
|:--|:---|:---|
| A1 | `.gitignore` 补协议 §五-1 起手条目（`*.local.md`、`.env.*`、密钥扩展名、`*.log`、AI 工具目录、`desktop.ini`），`git check-ignore -v` 抽查生效 | §五-1 |
| A2 | 仓库级身份补齐：`git config user.name Samjossi`（email 已是匿名 noreply，核对保留） | §五-2 |
| A3 | 新建 `scripts/privacy_scan.sh`：身份校验 + tracked 文件内容扫描（正则与豁免规则按协议 §五-3 原文，规则单点维护） | §五-3 |
| A4 | `scripts/check.sh` 首段并入隐私扫描（先于 ruff/pytest 执行） | §五-3 |
| A5 | 新建 `.githooks/pre-commit`（只调 `privacy_scan.sh`），本机 `git config core.hooksPath .githooks` | §五-4 |
| A6 | 双向验收（§六）：正向放行 + 负向造泄漏实测拦截（pre-commit 与 check.sh 各验一次），验收后清理测试文件 | §六 |

### 阶段 B：存量文件脱敏（当前工作区）

- `tests/fixtures/reasonix_frames.jsonl`：`/home/<真实用户名>/.../Zen_Studio` → `<项目根目录>`，`/home/<真实用户名>/.reasonix` → `<用户目录>/.reasonix`；
- 脱敏后跑全量测试，确认回放该 fixture 的用例不受影响（路径在断言中应无语义作用，若有则同步调整断言）。

### 阶段 C：历史重写（清除已入库泄漏，🔴 破坏性，执行前需用户当场确认）

`reasonix_frames.jsonl` 的泄漏已在历史（`a9554c0`），删当前文件无效。按 §七：

1. `git bundle create .temp/backup-prerewrite.bundle --all` 备份；
2. 替换规则文件放 `.temp/`（含真实值，不入库）：真实路径 → 占位符；
3. `git filter-repo --replace-text` 重写（提交身份本就匿名，无需 name/email callback）；
4. 三零复查：全历史 blob 零命中、提交信息零命中、身份只剩匿名；
5. 重建 origin/github 远程并**分别强推**（Forgejo 与 GitHub 都收过 `a9554c0`）；
6. 远端验证无误后删备份。

⚠️ 单开发者仓库、无双轨协作者，强推风险可控，但仍须用户明确拍板后执行。

### 阶段 D：托管侧加固（治本，需服务端权限，另行安排）

- Forgejo（NAS）服务端 `pre-receive` 扫描脚本；
- GitHub 仓库开启 push protection（secret scanning）；
- 主分支保护（禁止强推与阶段 C 的强推需求冲突——阶段 C 完成后再开）。

## 4. 验收标准

1. 阶段 A 后：干净工作区 `bash scripts/check.sh` 通过；造泄漏文件 `git add -f` 后 check.sh 与 `git commit` 均被拦；清理后恢复通过；
2. 阶段 B 后：存量 tracked 扫描零 🔴 命中（占位符命中豁免），全量测试绿；
3. 阶段 C 后（若执行）：三零复查通过，双远程强推完成；
4. 全程遵守：扫描规则只增不减、豁免带 why 注释。

## 5. 风险与注意

- ⚠️ 阶段 C 重写历史不可逆，bundle 备份是唯一后悔药；强推前须确认两个远程无他人克隆协作；
- ⚠️ `隐私脱敏与扫描阻断通用协议` 文件名豁免在本仓库实际无命中（`协议和指南/` 整体不入库），但按协议 §九-1 规则只增不减，豁免规则原样保留；
- 本计划完成（或阶段拆分定案）后归档至 `文档/修改记录/`。
