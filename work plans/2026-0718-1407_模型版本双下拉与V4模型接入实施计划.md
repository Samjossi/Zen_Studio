# 模型/版本双下拉与 V4 模型接入实施计划

> **状态**：草稿
> **范围**：`llm/providers/deepseek.py` — V4 模型接入 + 思考模式参数化；`gui/panels/chat/model_bar.py` — 模型/版本双下拉
> **时间**：2026-07-18 14:07（设计，UTC+8）
> **优先级**：高

---

## 1. 目标

将聊天面板的版本切换升级为"模型 + 版本"双下拉结构，并接入 DeepSeek V4 系列模型。前置计划：[文档/修改记录/2026-0718-1335_模型版本切换与思考块实施计划.md](文档/修改记录/2026-0718-1335_模型版本切换与思考块实施计划.md)（单下拉版已落地归档）。

## 2. 官方文档核实结论（[api-docs.deepseek.com](https://api-docs.deepseek.com/zh-cn/)，2026-07-18 时查）

### 2.1 模型清单

| 模型 ID | 性质 | 思考模式 | 备注 |
|:---|:---|:---|:---|
| `deepseek-v4-flash` | 正式（DeepSeek-V4-Flash） | 支持思考/非思考，**默认思考** | 输入 1 元/M、输出 2 元/M，并发 2500 |
| `deepseek-v4-pro` | 正式（DeepSeek-V4-Pro） | 支持思考/非思考，**默认思考** | 输入 3 元/M、输出 6 元/M，并发 500 |
| `deepseek-chat` | 遗留别名 = v4-flash **非思考** | — | ⏳ 2026/07/24 23:59 弃用 |
| `deepseek-reasoner` | 遗留别名 = v4-flash **思考** | — | ⏳ 2026/07/24 23:59 弃用 |

### 2.2 思考模式参数化（V4 关键架构变化）

- 开关：`extra_body={"thinking": {"type": "enabled" | "disabled"}}`（OpenAI SDK 写法），**默认 enabled**
- 强度：`reasoning_effort: "high" | "max"`（`low`/`medium` 映射为 `high`，`xhigh` 映射为 `max`；普通请求默认 `high`）
- 思维链仍经流式 `delta.reasoning_content` 返回——**现有 Chunk 解析代码继续兼容，无需改动**
- 思考模式下 `temperature`/`top_p` 等采样参数不生效（设置不报错）
- ⚠️ 认知更正：多轮拼接中，无工具调用时 `reasoning_content` 回传会被 API **忽略**（非 400；仅工具调用轮次必须回传否则 400）。现有"思维链剥离不入历史"策略继续安全，前次归档文档中"回传会 400"的表述应以此为准

## 3. 架构设计

核心变化：思考模式从"独立模型 ID"变为"请求参数"，版本项数据结构升级为 **模型 ID + 思考开关** 的组合。

```
llm/providers/deepseek.py     # 改：MODELS 重构为版本项列表（api_id + alias + thinking + deprecated）
                              #     chat() 按当前版本构造 extra_body thinking 参数
gui/panels/chat/model_bar.py  # 改：单 QComboBox → 双 QComboBox（模型 | 版本），模型变更刷新版本列表
```

### 3.1 版本项数据结构

```python
@dataclass(frozen=True)
class ModelVersion:
    api_id: str        # API 模型 ID
    alias: str         # 版本别名（UI 显示）
    thinking: bool     # 思考模式开关（v4 经 extra_body 下发；旧别名隐含模式，不下发）
    legacy: bool = False    # 旧别名标记（UI 显示弃用提示）
    send_thinking: bool = True  # 是否下发 thinking 参数（旧别名为 False）

MODELS: tuple[ModelVersion, ...] = (
    ModelVersion("deepseek-v4-flash", "V4 Flash · 思考", thinking=True),
    ModelVersion("deepseek-v4-flash", "V4 Flash · 非思考", thinking=False),
    ModelVersion("deepseek-v4-pro", "V4 Pro · 思考", thinking=True),
    ModelVersion("deepseek-v4-pro", "V4 Pro · 非思考", thinking=False),
    ModelVersion("deepseek-chat", "chat（旧版，07/24 弃用）", thinking=False, legacy=True, send_thinking=False),
    ModelVersion("deepseek-reasoner", "reasoner（旧版，07/24 弃用）", thinking=True, legacy=True, send_thinking=False),
)
DEFAULT_VERSION = MODELS[0]  # V4 Flash · 思考（官方默认即思考）
```

> 说明：旧别名（chat/reasoner）的模式由服务端按别名映射处理，客户端**不下发** `thinking` 参数，避免与别名语义冲突；07/24 弃用后从 `MODELS` 移除即可。

### 3.2 组件职责

| 组件 | 职责 |
|:---|:---|
| `ModelVersion`（dataclass） | 版本项：API ID + 显示别名 + 思考开关 + 旧版标记 |
| `DeepSeekLLM.set_version(index \| api_id, thinking)` | 切换当前版本项；`chat()` 按 `send_thinking` 决定是否携带 `extra_body={"thinking": {"type": ...}}` |
| `label_for(version)` | 显示格式单点维护（沿用现有约定） |
| `ModelBar` | 双下拉：左「模型」（本期仅 DeepSeek 一项，占位未来多 provider）+ 右「版本」（6 项）；模型变更时刷新版本列表 |
| 思考强度 | 本期不显式传 `reasoning_effort`（走服务端默认 `high`），UI 暴露强度选择列为后续 |

### 3.3 关键行为约定

- 默认版本：**V4 Flash · 思考**（`deepseek-v4-flash` + thinking enabled，对齐官方默认）
- 旧版别名选中后正常可用至 2026/07/24，UI 别名自带弃用提示，不做额外弹窗
- 切换语义不变：写 provider 单例状态、下次请求生效、发送中双下拉均锁定
- 思维链渲染链路（Chunk → Worker → Output 灰斜体）零改动

## 4. 功能裁剪（本期）

- ✅ 模型/版本双下拉（模型项本期仅 DeepSeek，占位）
- ✅ 版本项 6 个：V4 Flash/Pro × 思考/非思考 + 2 个旧版别名（带弃用标记）
- ✅ v4 思考模式经 `extra_body` 参数化；旧别名不下发 thinking 参数
- ✅ 默认版本切换为 V4 Flash · 思考
- ❌ 剔除：`reasoning_effort` 强度选择 UI（后续）、选中版本持久化（后续）、07/24 后旧别名清理（到期处理）

## 5. 实施步骤

| 步骤 | 内容 | 产出 |
|:---|:---|:---|
| 1 | 改 [`llm/providers/deepseek.py`](llm/providers/deepseek.py)：新增 `ModelVersion` dataclass；`MODELS` 重构为版本项 tuple；`set_model()` 改为按版本项切换（兼容索引/api_id）；`chat()` 按 `send_thinking` 构造 `extra_body`；`label_for()` 适配新结构 | V4 接入 |
| 2 | 改 [`gui/panels/chat/model_bar.py`](gui/panels/chat/model_bar.py)：单下拉改双下拉（模型 \| 版本），版本项从 `MODELS` 填充（`label_for`），`model_changed` 信号携带版本项标识 | 双下拉 UI |
| 3 | 改 [`gui/panels/chat/panel.py`](gui/panels/chat/panel.py)：`_on_model_changed` 适配新版本项切换接口 | 面板适配 |
| 4 | 更新 [`llm/README.md`](llm/README.md)（MODELS 新结构、thinking 参数、认知更正）与 [`gui/README.md`](gui/README.md) 第 4 节（双下拉） | 文档一致 |
| 5 | 走查验证（见第 6 节） | 验证通过 |

## 6. 验证标准

| 项 | 标准 |
|:---|:---|
| 显示 | 输入区顶行双下拉：`模型 [DeepSeek▾] 版本 [V4 Flash · 思考▾]`，默认选中 V4 Flash · 思考 |
| 版本项 | 版本下拉共 6 项，旧版两项别名含"07/24 弃用"标记 |
| 思考模式 | 选 V4 Flash · 思考发问：思维链灰斜体上屏后接正文；选 V4 Flash · 非思考：直接正文无思维链 |
| V4 Pro | 选 V4 Pro · 思考发问：正常流式回复（验证 api_id 正确） |
| 旧版兼容 | 选 chat（旧版）：正常回复无思维链；选 reasoner（旧版）：思维链上屏（不弃用前行为不变） |
| 多轮 | V4 Flash · 思考模式两轮对话，第二轮感知上下文且无报错 |
| 锁定 | 发送中双下拉均禁用 |
| 走查 | `uv run main.py` 无报错；`--auto-screenshot --screenshot-on-start` 截图确认双下拉渲染 |

## 7. 风险与对策

| 风险 | 等级 | 对策 |
|:---|:---:|:---|
| v4 思考模式默认开启，用户切到"非思考"版本项后参数未生效 | 🟡 | 验证标准要求双模式各测一次；`send_thinking` 逻辑覆盖 v4 全部 4 项 |
| 旧别名与 `thinking` 参数同传导致服务端行为未定义 | 🟡 | `send_thinking=False` 从结构上隔离；验证标准含旧版回归 |
| 默认改 V4 后计费/延迟变化（思考模式首 token 慢、pro 单价高） | 🟢 | 默认项选 Flash 而非 Pro；非思考版本项一键可切 |
| 07/24 旧别名弃用后选中项失效 | 🟢 | 弃用标记已上 UI；到期后从 `MODELS` 移除两项即可，列入后续 |
| `extra_body` 写法与 openai SDK 版本兼容性 | 🟢 | 官方文档明确 OpenAI SDK 经 `extra_body` 传 thinking；冒烟验证确认 |

## 8. 后续阶段（不在本计划内，仅备案）

1. `reasoning_effort` 思考强度选择（high/max）UI 暴露
2. 选中模型/版本持久化到 `config/settings.json`
3. 2026/07/24 后移除 `deepseek-chat`/`deepseek-reasoner` 旧别名版本项
4. 模型下拉接入第二家 provider（需对应 API key）后启用分组/联动刷新
