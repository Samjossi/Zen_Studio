# 「最近打开的文件」→「最近打开的项目」改造计划

> **状态**：待实施
> **范围**：[`gui/menus/file_menu.py`](../../gui/menus/file_menu.py)（文案 + 子菜单语义）
>   + [`gui/main_window.py`](../../gui/main_window.py)（记录接线 + 回放槽）
>   + [`gui/window_state.py`](../../gui/window_state.py)（键空间回收 `recent_files`）
>   + [`gui/panels/viewer/panel.py`](../../gui/panels/viewer/panel.py)（删除 `file_opened` 信号）
>   + 新增 `gui/recent_projects.py`（最近项目存取，全局共享）
>   + 删除 `gui/recent_files.py`（最近文件存取，整体退役）
> **时间**：2026-07-24 10:03（立项，UTC+8）
> **起因**：需求方评审文件菜单「最近打开的文件」，指出记录语义不符合 IDE
>   惯例——IDE 记住的是**最近打开的项目（文件夹）**，而非单独的文件。
>   决定将该项改造为「最近打开的项目」

## 目标布局（仅子菜单语义与文案变化，菜单位置/助记符不变）

```
文件(&F)
├─ 打开文件(&O)…            （现有，不动）
├─ 新建窗口(&N)             （现有，不动）
├─ 在新窗口打开文件夹(&W)…  （现有，不动）
├─ 打开配置目录(&C)         （现有，不动）
├─ 最近打开的项目(&R) ▸     （改造：记录项目文件夹，点击在新窗口打开）
├─ ────────────
└─ 退出(&Q)                 （现有，不动）
```

---

## 1. 现状与差距

现有「最近打开的文件」（work plans/2026-0722-1901 落地）记录的是
**单文件绝对路径**（`ViewerPanel.file_opened` 信号收口），回放为在查看器
重新打开该文件。与 IDE 惯例的差距：

1. **记录对象错误**：IDE 的 Recent 列表回答的问题是"我最近在做哪几个
   项目"，用于快速回到项目现场；单文件列表回答的是"我最近看过哪几个
   文件"，回访价值低（文件树一点即达，无需历史）。
2. **回放层级错误**：项目级回放应恢复整个工作区现场（新窗口绑定该
   工作区根），而非只在中栏查看器里摆一个文件。

### 1.1 关键既有事实（方案约束来源）

| 事实 | 位置 | 对本计划的约束 |
|:--|:--|:--|
| 多开模型：一进程绑定一工作区根，「打开文件夹」= `subprocess.Popen` 起新进程 | `main_window.py:343-359` | 项目回放天然复用 `_spawn_window(folder)`，零新增进程逻辑 |
| 最近文件存取按工作区隔离（`window_state_<hash8>.json`） | `recent_files.py` `window_state.py:29-32` | **项目列表必须全局共享**——按工作区隔离时每个窗口只会记录自己，列表永远只有 1 条，语义上即错误（见 §2.1） |
| `ViewerPanel.file_opened` 唯一消费方是 `recent_files.add` | `main_window.py:157`（全仓 grep 确认） | 记录功能退役后信号成死代码，一并删除（FileExplorer / ChangesPanel 的同名信号有其他消费方，不动） |
| `window_state`「未登记键读取即丢弃，不写回」 | `window_state.py:114` | 回收 `recent_files` 键后存量状态文件中的旧键自然失效，**零迁移代码** |
| `write_json_atomic` 原子写已存在 | `gui/settings.py` | 全局最近项目文件的写盘复用之，不自造 IO |
| 子菜单 aboutToShow 动态重建 + 占位/清除项模式已成熟 | `file_menu.py:50-71` | 仅换数据源与回放动作，交互骨架原样沿用 |
| 记录点候选：`open_folder_in_new_window` 只覆盖菜单入口 | `main_window.py:343` | 命令行 `main.py <folder>` 启动不入列——收口点上移至**窗口启动**（见 §2.2） |

## 2. 方案

### P1 全局存取层（新增 `gui/recent_projects.py`）

**存储位置：`config/recent_projects.json`（全局单文件，不按工作区哈希分文件）。**

- 不放 `settings.json`：`reset_settings` 全量重写偏好文件，最近项目列表
  是历史数据而非偏好，不应随"恢复默认设置"被清
- 不放 `window_state` 体系：该体系按工作区根哈希分文件，无"全局"位置；
  且键空间由 `WindowState` 定型为布局 + 本工作区查看历史，混入全局
  数据破坏其"按工作区隔离"的单一职责

**文件格式**（dict 包一层，留扩展位；键名常量引用，AFCP 3.1）：

```json
{"recent_projects": ["/abs/path/to/project", "..."]}
```

**存取层**：

```python
MAX_RECENT_PROJECTS = 10

class RecentProjectsStore:
    """最近打开的项目（工作区根）列表，全局共享，存 config/recent_projects.json。

    add(path)：规范化（resolve）+ 去重 + 置顶 + 截断 10 条，即时原子写；
    list()：读回（缺失/损坏回退空表，拷出隔离）；
    remove(path)：回放发现目录已消失时剔除；
    clear()：清空（子菜单「清除列表」项）。
    """
```

- 构造显式接 `state_file: Path`（AFCP 2.3 依赖显式；调用方传
  `CONFIG_DIR / "recent_projects.json"`）
- 读写走「读全量 → 合并 → `write_json_atomic` 写回」链；损坏防御：
  非 dict / 键值非 `list[str]` 一律回退 `[]`（路径允许多字节 UTF-8，
  与 `recent_files` 判据同理）
- 多开并发：各窗口启动写同一文件，读-合并-写后写胜；列表非关键数据，
  收敛即正确，不加锁（沿用 2026-0722-1901 §5 论证）

### P2 记录链与回放改造

**记录点：窗口启动即记录自身工作区根（收口于 `MainWindow.__init__`）。**

- 覆盖全部打开路径：菜单「在新窗口打开文件夹」、命令行
  `main.py <folder>`、「新建窗口」（同根）——三路径最终都汇聚为
  "一个窗口进程带着一个 workspace_root 启动"
- 去重置顶语义下，「新建窗口」/重复打开同根只是把该项固定在首位，
  不产生噪音条目
- 默认根（PROJECT_ROOT）照常记录：它也是合法项目，且是最高频现场
- 时机取 `_build_layout` 之后即可（无 UI 依赖，纯数据写盘）

**回放槽 `MainWindow.open_recent_project(path)`**：

- `Path.is_dir()` 探活——存在则 `_spawn_window(path)` 起新进程绑定该
  工作区根（新窗口启动时会自动把该项置顶，无需手工再记）
- 目录已消失：`store.remove(path)` + 状态栏提示
  「文件夹已不存在，已从列表移除」（沿用原最近文件的提示风格）
- 点击项恰为当前工作区根：等价于「新建窗口」，**接受、不开特例**

### P3 旧功能退役与清理

| 清理项 | 说明 |
|:--|:--|
| 删除 `gui/recent_files.py` | 功能整体退役，无存留价值 |
| `window_state.py` 键空间 6→5 | 移除 `KEY_RECENT_FILES`；`_is_valid_value` 按键分流撤销，恢复布局键单判据（`None \| ASCII str`）；存量状态文件中的 `recent_files` 旧键经「未登记键读取即丢弃」自然失效，零迁移代码 |
| 删除 `ViewerPanel.file_opened` 信号 | 唯一消费方（最近文件记录）退役后成死代码；FileExplorer / ChangesPanel 的同名信号有 Git 统计等消费方，**保留不动** |
| `main_window.py` 换装 | 移除 `RecentFilesStore` 装配与 `open_recent_file` 槽；新增 `RecentProjectsStore` 装配（启动记录）与 `open_recent_project` 槽 |
| `file_menu.py` 文案与注册键 | 「最近打开的文件（&R)」→「最近打开的项目（&R)」（助记符不变）；注册键 `file.recent_files` → `file.recent_projects`；子菜单显示名改 `文件夹名  —  父目录`，toolTip 全路径；「（空）」占位与「清除列表（&L)」沿用 |
| 文档同步 | `gui/README.md` 接线说明、`file_menu.py` / `main_window.py` / `window_state.py` / `viewer/panel.py` 模块 docstring 同步新语义 |

## 3. 任务分解

| # | 任务 | 产出 |
|:--|:--|:--|
| 1 | P1：`gui/recent_projects.py` 存取层（add/list/remove/clear，上限 10，损坏防御） | recent_projects.py（新增） |
| 2 | P2a：`MainWindow` 装配 store + 启动记录自身根 + `open_recent_project` 探活回放槽 | main_window.py |
| 3 | P2b：子菜单改造（文案/显示名/回放动作），注册键改名 | file_menu.py |
| 4 | P3a：删除 `gui/recent_files.py`；`window_state.py` 键空间 6→5、分流撤销 | recent_files.py（删除）+ window_state.py |
| 5 | P3b：删除 `ViewerPanel.file_opened` 信号及其发射点 | viewer/panel.py |
| 6 | P3c：docstring 与 `gui/README.md` 同步 | 各模块头注释 + README |
| 7 | 验证（见 §4） | .temp 冒烟脚本 |

## 4. 验证方案

1. **冒烟脚本**（`.temp/smoke_recent_projects.py`，offscreen）：
   - 存取层：add 规范化/去重/置顶/11 条截断 10；落盘回读一致；
     损坏防御（str/dict/混合 list/非 dict 文件）回退 `[]`
   - 启动记录：以 `.temp/smoke_ws_a`、`.temp/smoke_ws_b` 为根分别实例化
     MainWindow → 全局文件含两条且 b 置顶；再次启动 a → a 置顶、无重复
   - 子菜单重建：空列表占位「（空）」禁用；有记录时逐条生成
     `文件夹名 — 父目录`、toolTip 全路径；「清除列表」启停正确
   - 回放：存在目录 → `_spawn_window` 打桩断言以该根起进程；不存在目录
     → 从列表剔除 + 状态栏提示
   - 注册表：`file.recent_projects` 存在、`file.recent_files` 不存在
   - 退役断言：`window_state` 键空间回 5 键；含 `recent_files` 旧键的存量
     状态文件读回时旧键被静默丢弃、布局 5 键不受影响
2. **多开手测**：两个不同根的窗口互相在对方「最近打开的项目」里可见；
   同根「新建窗口」后列表仅置顶、不新增
3. **`check_compliance.py` + 既有冒烟全量回归**（重点：
   `smoke_file_menu.py` 中断言旧语义的部分需随改造同步更新）
4. **实机走查**：菜单观感 + 点击最近项目实开新窗口 + 文件夹改名后回访
   走剔除路径

## 5. 风险与回滚

| 风险 | 评估与对策 |
|:--|:--|
| 删除 `ViewerPanel.file_opened` 误伤其他消费方 | 已全仓 grep 确认唯一消费方为 `main_window.py:157`；冒烟与回归兜底 |
| 全局文件并发写（多窗口同时启动） | 读-合并-写窗口为毫秒级；列表非关键数据，后写胜收敛即正确 |
| `reset_settings` 误清最近项目 | 存储独立于 settings.json，重置偏好不触碰本文件（P1 选址论证） |
| 冒烟工作区污染真实全局列表 | 冒烟脚本对 `CONFIG_DIR` 或 store 的 `state_file` 打桩指向 `.temp/`，结束即删 |
| 旧最近文件列表中的记录去向 | 随功能退役自然废弃（存量状态文件旧键静默失效）；不接受"迁移为最近项目"——单文件路径无法逆推用户意图的项目根 |
| 回滚 | 新增一文件 + 改四处 + 删一文件，`git revert` 即还原；旧功能代码在 git 历史中完整可溯 |

## 6. 交付前自查

- [ ] 零硬编码色值（本计划无 UI 样式新增）
- [ ] 键名一律 `KEY_*` 常量引用，无裸字符串键（AFCP 3.1）
- [ ] 依赖显式：`RecentProjectsStore` 构造显式接 `state_file`（AFCP 2.3）
- [ ] `check_compliance.py` 全过
- [ ] 冒烟脚本落 `.temp/`（项目内临时目录纪律）
- [ ] 真实平台走查一次——需求方实机复核
