> **状态**：已实施（offscreen 验证通过 2026-08-02 15:19，待真机自然操作终裁）
> **范围**：`core/git/`（新增 log 数据源）、`gui/git_graph_dialog.py`（新建）、`gui/menus/view_menu.py`、`gui/main_window.py`
> **时间**：2026-08-02 15:07（设计，UTC+8）
> **优先级**：低

# Git 提交历史图弹窗显示计划

> 在「视图」菜单新增一个动作，点击弹出非模态对话框，以只读文本图（`git log --graph` 的 ASCII 拓扑）展示当前仓库的提交历史（分支/合并/标签装饰）。不占用主布局，不提供图内交互，第一版只做纯展示。

## 1. 目标行为

| 场景 | 现状 | 目标 |
|:---|:---|:---|
| 查看提交历史拓扑 | ❌ 无入口，须切外部终端敲 `git log --graph` | 视图菜单 ▸ 「提交历史图(&L)」弹窗展示 |
| 分支/合并/HEAD/标签装饰 | — | 随 `--graph --decorate --all` 原样呈现 |
| 仓库有新提交后查看 | — | 弹窗内「刷新」按钮手动重拉；弹窗每次打开亦自动重拉 |
| 无 git / 非仓库工作区 | — | 弹窗显示降级占位文本（不报错、不弹错误框），对齐全局静默降级语义 |
| 主窗口布局 | 三栏 splitter 定式 | 零改动（对话框为独立顶级窗口） |

## 2. 现状盘点（实证）

### 2.1 已有机制（复用点）

| 位置 | 内容 | 处置 |
|:---|:---|:---|
| `core/git/runner.py` `run_git(repo_dir, *args)` | git CLI 封装：2s 超时、失败/超时返回 None 静默降级、`-c core.quotepath=false` 中文路径原样输出 | 直接复用，零改动 |
| `core/git/runner.py` `git_available()` / `find_repo_root()` | 环境检测 | 复用（弹窗打开时探测） |
| `core/git/service.py` `GitStatusService.is_enabled` / `repo_root` | 仓库可用性单一判定，已由 `GitStatusController` 持续维护 | 复用其结论做降级分支，不重复探测逻辑进 UI 常驻路径 |
| `gui/main_window.py` `_settings_dialog` | 「非模态单例、首次打开惰性创建」对话框先例（设置中心） | 提交历史图弹窗照搬同一模式 |
| `gui/menus/view_menu.py` 「刷新 Git 状态(&G)」「刷新文件树(&F)」 | 非勾选动作 + `actions.register(...)` 注册先例 | 新动作照搬同一装配模式 |
| `gui/menus/assembler.py` | 菜单构建顺序即菜单栏顺序 | 无需改动，view_menu 内部加项即可 |
| `gui/popups.py` 头部规约 | 透明化仅针对 QMenu / QComboBox 弹出层 | QDialog 不受此规约约束，无需透明化处理 |

### 2.2 缺口（新建点）

| 缺口 | 说明 |
|:---|:---|
| 提交图数据源 | `core/git/` 下无 log 查询模块，需新建 `core/git/log.py` |
| 展示对话框 | 无通用只读文本弹窗，需新建 `gui/git_graph_dialog.py` |
| 菜单入口 | 视图菜单无此项 |

## 3. 设计要点

### 3.1 数据源：一行命令，原样透传

```python
# core/git/log.py
run_git(repo_root, "log", "--graph", "--oneline", "--decorate", "--all",
        "--color=never", "-n", str(MAX_COUNT))
```

- **原样透传 stdout，不做结构化解析**：`--graph` 的 ASCII 线条（`*` `|` `\` `/`）在等宽字体下自对齐，解析反而引入错排风险。本项目第一期为纯展示，无交互需求，无解析必要。
- **`--all` 覆盖全部分支/标签/stash 引用**，对齐 Git Graph 类工具的默认视野。
- **`-n MAX_COUNT`（取 500）条数上限**：大仓库一次拉全量可能撞 runner 的 2s 超时上限并返回大文本；500 条对纯浏览足够。命中上限时在文本尾部追加一行提示（如 `……仅显示最近 500 条`），让用户知晓截断而非误以为仓库只有这些提交。
- **`--color=never` 显式去色**：防御用户全局 gitconfig 设了 `color.ui=always` 导致 ANSI 转义序列混入文本（runner 管道捕获时 git 默认已去色，此行是显式保险）。
- **失败返回 None**：沿用 runner 静默降级语义，由 UI 层决定占位呈现。

### 3.2 对话框：非模态单例 + 等宽只读文本

- **载体**：`QDialog` + 只读 `QPlainTextEdit`（等宽字体）。`QPlainTextEdit` 对大块纯文本的渲染开销低于 `QTextBrowser` 富文本路径，且天然无链接/选中富格式干扰；等宽字体是 `--graph` 线条对齐的前提。
- **非模态（`show()`）+ 单例惰性创建**：照搬 `_settings_dialog` 模式——MainWindow 持 `self._git_graph_dialog: GitGraphDialog | None = None`，菜单触发时惰性创建并 `show()`/`raise_()`/`activateWindow()`。用户可一边看图一边回主窗口操作。
- **对话框结构**：顶部信息行（仓库根 + 当前分支名）+ 中部文本区 + 底部「刷新」按钮。当前分支名用 `run_git(repo_root, "branch", "--show-current")` 获取（detached HEAD 时为空串，显示 `(detached)`）。
- **刷新时机两处**：每次 `show()` 时自动重拉一次；对话框内「刷新」按钮手动重拉。**不做事件驱动常驻刷新**（提交图是低频查看，不值得挂进 GitStatusController 的去抖汇流）。
- **降级占位**：`is_enabled` 为 False 或 `fetch` 返回 None 时，文本区置占位文案（「当前工作区不在 git 仓库内」/「提交历史获取失败」），信息行与刷新按钮照常可用（重试语义），不弹任何错误框。

### 3.3 菜单入口

- 视图菜单在「刷新 Git 状态(&G)」之后插入「提交历史图(&L)」非勾选动作，`triggered` 直连 MainWindow 的 `show_git_graph()`，`actions.register("view.git_graph", action)`。
- **菜单项不做 enable 联动**：非仓库时点击照样弹窗（窗内降级占位），避免给 GitStatusController.refresh 扇出链再加一个菜单状态同步点。低频次动作，窗内占位已足够告知。

### 3.4 字体与主题

- 等宽字体取自应用统一字体设置（沿用终端面板/查看器的等宽字体来源，实施时确认单一来源键，不新造字体配置）。
- 文本区颜色走主题 qss 全局规则（`QPlainTextEdit` 已被四主题覆盖），不新增主题键。

## 4. 决策表

| 编号 | 决策点 | 初步倾向 | 依据 |
|:---|:---|:---|:---|
| D1 | 渲染方式 | 文本图原样透传（非 QGraphicsView 泳道绘制） | 纯展示需求；矢量泳道绘制工作量与解析风险不在本期 |
| D2 | 模态性 | 非模态单例（照搬设置中心模式） | 看图时可回主窗口对照代码；项目已有同款先例 |
| D3 | 数据拉取时机 | 打开时 + 手动刷新，两处；不挂事件驱动 | 低频查看；避免污染 GitStatusController 去抖汇流 |
| D4 | 条数上限 | `-n 500` + 截断提示行 | runner 2s 超时是兜底不是体验保障；大仓库防护 |
| D5 | 菜单项 enable 联动 | 不做；窗内降级占位 | 避免 refresh 扇出链新增同步点；低频动作占位即可 |
| D6 | 非仓库/失败呈现 | 文本区占位文案，不弹错误框 | 对齐 runner「无 git 属正常场景而非故障」语义 |
| D7 | 控件选型 | `QPlainTextEdit`（非 QTextBrowser） | 大文本渲染开销低；无富文本干扰；等宽对齐 |

## 5. 任务分解

| 编号 | 任务 | 文件 | 要点 |
|:---|:---|:---|:---|
| T1 | 数据源：`fetch_commit_graph(repo_root) -> str \| None` | `core/git/log.py`（新建） | 单函数模块；命令与参数见 3.1；截断提示行在此拼接 |
| T2 | 对话框：信息行 + 只读等宽文本区 + 刷新按钮 + 降级占位 | `gui/git_graph_dialog.py`（新建） | 构造接收 `GitStatusService`（只读其 is_enabled/repo_root 结论）；`reload()` 内部调 T1 |
| T3 | MainWindow 挂接：单例惰性创建 + `show_git_graph()` | `gui/main_window.py` | 照搬 `_settings_dialog` 模式 |
| T4 | 视图菜单加项「提交历史图(&L)」 | `gui/menus/view_menu.py` | 位于「刷新 Git 状态(&G)」之后；注册 `view.git_graph` |
| T5 | 验证 | `.temp/` 脚本 + offscreen 截图 | 见 §6 |

## 6. 验证方案

1. **数据层单测**（`.temp/` 下脚本，项目 `.venv` 运行）：
   - 在 `.temp/` 内构造 fixture 仓库：主分支 3 提交 + 特性分支 2 提交 + 一次合并 + 一个标签，断言 `fetch_commit_graph` 输出含 `*`/`\` 图线、分支名装饰、标签装饰。
   - 空仓库（无提交）、非仓库目录、不存在路径三例，断言返回 None 不抛异常。
   - `-n` 截断：造 600 条提交的仓库（脚本批量空提交），断言输出尾部含截断提示行。
2. **offscreen GUI 冒烟 + 截图对照**（依 `视觉验证闭环开发指南.md`）：
   - `QT_QPA_PLATFORM=offscreen` 起对话框指向 fixture 仓库，grab 截图，断言图线字符渲染、等宽对齐无错排。
   - 降级分支截图：非仓库工作区下弹窗显示占位文案。
   - 四主题逐一截图目验文本区配色可读性。
3. **真机回归**：
   - 视图菜单点击 → 弹窗 → 主窗口照常可操作（非模态验证）。
   - 终端 `git commit` 后点弹窗「刷新」→ 新提交出现。
   - 再次点击菜单项 → 已有窗口置顶而非新开（单例验证）。

## 7. 不做的事

- ❌ 图内任何交互：点击提交看 diff、右键 cherry-pick/revert、双击切换分支（后续如需另行立项）。
- ❌ QGraphicsView 矢量泳道绘制（D1）。
- ❌ 结构化提交数据解析（无消费方；为解析而解析）。
- ❌ 事件驱动常驻刷新 / 纳入 GitStatusController 去抖汇流（D3）。
- ❌ 菜单项 enable 状态随仓库可用性联动（D5）。
- ❌ 分页/虚拟滚动加载更多（500 条上限内一次性呈现）。
- ❌ 提交搜索/过滤框（第一版纯浏览）。

## 8. 与既有文档关系

| 文档 | 关系 |
|:---|:---|
| `2026-0720-0135_Git文件装饰与简易差异指示方案选型.md` | 本项目 Git 能力的初始选型；本计划在其「core/git 数据层 + UI 薄呈现」分层之上追加只读查询能力，不动其状态/装饰语义 |
| `视觉验证闭环开发指南.md` | §6 验证流程遵循其截图闭环要求 |
| `文档编写规范.md` v3.0 | 本文档命名与格式依据 |

---

## 9. 实施记录

> **实施时间**：2026-08-02 15:19（UTC+8）

| 任务 | 落地 | 验证 |
|:---|:---|:---|
| T1 数据源 | `core/git/log.py`：`fetch_commit_graph`（`log --graph --oneline --decorate --all --color=never -n MAX_COUNT`）+ `count_commits`（`rev-list --all --count`）+ `current_branch`（`branch --show-current`） | 冒烟 19 项全过（图线/合并边/HEAD/分支/标签装饰、无 ANSI、总数、分支名） |
| T2 对话框 | `gui/git_graph_dialog.py`：信息行（仓库根 + 分支，detached 显示 `(detached 或未知)`）+ 只读等宽 `QPlainTextEdit` + 「刷新(&R)」；showEvent 自动重拉 + 按钮手动重拉；降级占位两档文案 | offscreen 断言 11 项全过（文本载入/装饰/信息行/单例/四主题非纯色/降级文案） |
| T3 挂接 | `gui/main_window.py`：`_git_graph_dialog` 单例惰性创建 + `show_git_graph()`（照搬 `_settings_dialog` 模式）；**追加** `_apply_font_size` 链挂 `refresh_font()`（计划外小扩，同查看器/终端先例，字号跟随全局） | 截图脚本内经 MainWindow 真实链路起窗验证 |
| T4 菜单 | `gui/menus/view_menu.py`：「刷新 Git 状态(&G)」后插入「提交历史图(&L)」，注册 `view.git_graph`；模块文档串同步 | 冒烟中经 `window.show_git_graph()` 链路验证；菜单装配无回归 |
| T5 验证 | 新增 `.temp/smoke_git_graph_1507.py`（19 断言）、`.temp/shot_git_graph_1507.py`（11 断言 + 四主题截图）；产物 `.temp/shots/gitgraph_1507_<theme>.png`、`_degraded.png` | **30 项断言全过；回归 `.temp/smoke_git_refresh_trigger.py` 全过零回退** |

**实施中发现与设计修正**：

1. **空仓库语义归一**（设计未预见）：`git log --all` 在无引用的空仓库退出码为 0、输出为空——不构成命令失败。数据层将空输出归一为 None，UI 与失败同一占位语义（尚无提交），避免空文本区困惑。
2. **非仓库 fixture 构造法**：fixture 目录位于项目（git 仓库）内，git 会向上发现外层仓库；空 `.git` 目录**无效**（git 校验不通过会继续上溯，实证），须用伪 gitfile（`.git` 文件写 `gitdir: /nonexistent`，git 命中即 fatal 终止发现）。
3. **截断验证手段调整**：以 `MAX_COUNT` 临时调小至 5（fixture 共 6 提交）验证提示行与显示条数，替代造 600 提交——同一代码路径，成本更低。
4. **字号跟随**（计划 3.4「实施时确认单一来源键」的落地结论）：等宽字体取 `get_mono_family()`、字号跟随 app 全局字号，与查看器/终端同路径；未新增任何配置键/主题键。

**截图闭环**：`.temp/shots/gitgraph_1507_cloud.png` 目验——合并拓扑 `|\`/`| *`/`|`/` 等宽对齐无错排，`(HEAD -> main, tag: v1)`、`(feature)` 装饰完整；四主题（cloud/wheat/sky/mint）逐一目验文本区配色可读；`gitgraph_1507_degraded.png` 目验——信息行「（非 git 仓库）」+ 占位文案「当前工作区不在 git 仓库内。」，刷新按钮可用。

**决策落地确认**：D1–D7 均按初步倾向落地，无反转；T3 追加字号链挂接（D2 先例模式的必要补全，非决策变更）。

**待办**：真机自然操作终裁（视图菜单点击 → 弹窗非模态操作主窗口 → 终端提交后「刷新」出新提交 → 再次点击菜单置顶而非新开）由用户日常使用中顺手确认，确认后本文档移入 `文档/修改记录/` 归档。
