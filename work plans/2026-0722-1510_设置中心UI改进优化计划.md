# 设置中心 UI 改进优化计划

> **状态**：已实施（P0/P1 同批交付；P2 窗口图标清理按裁决后置）
> **范围**：[`gui/settings_dialog.py`](../../gui/settings_dialog.py)（整体重构）
>   + [`gui/theme.py`](../../gui/theme.py)（muted_text 令牌引用，不改主题文件本体）
> **时间**：2026-07-22 15:10（设计）/ 2026-07-22 15:35（实施）
> **优先级**：中（纯 UI/交互打磨，零业务逻辑变更；不改任何设置键、不动
>   MainWindow 应用槽、不动 _PAGE_REGISTRY 机制）
> **依据**：需求方提供「AI 工具权限」页截图一张 + 对五页代码的逐项核查
> **承接**：[`work plans/2026-0722-1344_设置菜单简化与设置中心唯一配置面实施计划.md`](2026-0722-1344_设置菜单简化与设置中心唯一配置面实施计划.md)（页面注册表机制已落地，本计划只做注册表之上的表现层优化）

---

## 1. 现状缺陷清单

### 1.1 截图观察（「AI 工具权限」页）

| # | 缺陷 | 截图证据 | 级别 |
|:--|:---|:---|:---:|
| D1 | 左侧导航 140px 硬编码，「AI 工具权限」6 字选中态贴边拥挤，呼吸感不足 | 首项蓝底白字几乎顶满列宽 | 🟡 |
| D2 | 黑名单折叠按钮 `checkable QPushButton` 拉满整行，视觉上像横幅/输入框而非按钮，且无 ▶/▼ 折叠态指示 | 「查看危险命令黑名单…」一长条灰框 | 🟡 |
| D3 | 底部按钮为英文 `Close` 且自带红色 ✕ 图标，孤悬右下角，与全中文 UI 不一致 | 右下角「✕ Close」 | 🟡 |
| D4 | 右侧无页标题，切页后无「当前在哪一页」的明确指示，仅靠左导航选中态 | 内容区顶部直接是一行灰色小字 | 🟡 |
| D5 | 页面描述文字缩进 `setIndent(24)` 魔法数，与 radio 圆形指示器视觉不对齐 | 描述行比 radio 圈右偏约半字 | 🟢 |
| D6 | 说明灰 `color: gray` 硬编码，不走主题 `muted_text` 令牌，深色主题下对比度不可控 | —（代码核查） | 🟡 |
| D7 | 窗口标题栏残留默认应用图标，「设置中心」标题旁有无关小图 | 标题栏左侧小图标 | 🟢 |

### 1.2 代码核查（其余四页与骨架）

| # | 缺陷 | 位置 | 级别 |
|:--|:---|:---|:---:|
| D8 | 五页布局风格不统一：权限/终端/高级用 `QVBoxLayout`，模型/外观用 `QFormLayout`，控件对齐、缩进、说明行位置各写各的 | `_build_*_page` | 🟡 |
| D9 | 终端页仅 1 个复选框 + 1 行说明，660×480 窗口下大面积空旷；`resize(660, 480)` 硬编码且无 `minimumSize`，可拖到控件被裁 | `__init__` / `_build_terminal_page` | 🟡 |
| D10 | 导航与内容区之间无分隔线、无内边距规范，左右两栏视觉粘连 | `__init__` 布局装配 | 🟢 |
| D11 | radio 粗体 `font-weight: bold` 内联 style 硬编码；hint 样式常量 `_STYLE_HINT`/`_STYLE_DANGER` 与 qss 令牌体系割裂 | `_build_permission_page` | 🟢 |
| D12 | 切页/改项反馈不一致：仅权限页有状态栏消息，外观/终端/模型页静默生效 | `_on_*` 系列槽 | 🟢 |
| D13 | 黑名单 `QTextEdit` 展开后无固定高度策略，窗口被纵向撑变形；只读区无等宽字体，正则条目可读性差 | `_build_permission_page` | 🟢 |

## 2. 设计方案

### 2.1 骨架重排（P0）

```text
┌────────────────────────────────────────────────────┐
│ 设置中心                                            │
├──────────┬─────────────────────────────────────────┤
│ 导航      │ 页标题（粗体大字）                        │
│ （图标+   │ ───────────────（分隔线）                │
│  文字，   │                                         │
│  160px，  │ 页面内容区（统一外边距/间距常量）          │
│  分隔线） │                                         │
│          │                                         │
├──────────┴─────────────────────────────────────────┤
│                                          [关闭]    │
└────────────────────────────────────────────────────┘
```

- **页标题区**：`_PAGE_REGISTRY` 扩为四元组 `(导航名, 页标题/副标题, build, reload)`——
  副标题即现行页顶灰色 hint 上移，标题粗体 14pt、副标题 muted_text，
  `currentRowChanged` 时骨架统一刷新；各页 `_build_*_page` 不再自带页顶说明（D4/D8）
- **导航栏**：140 → 160px 且改 `setMinimumWidth`（不再 Fixed），项高 32px、
  左内边距 12px；右缘 1px `border` 令牌分隔线；图标列入裁剪（§4），本轮纯文字（D1/D10）
- **底部按钮**：弃用 `QDialogButtonBox`（其本地化依赖 QTranslator，未装载故英文），
  改普通 `QPushButton("关闭")` + `clicked.connect(self.reject)`，无图标（D3）
- **尺寸纪律**：`setMinimumSize(600, 420)`，初始 `resize(680, 500)`；
  内容区统一 `setContentsMargins(16, 12, 16, 12)` + `setSpacing(8)`（D9）

### 2.2 样式令牌化（P0）

- `_STYLE_HINT` 由 `color: gray` 改引主题 `muted_text` 令牌：settings_dialog 新增
  模块级 `_apply_dialog_palette(theme_tokens)` 挂入 `switch_theme` 收敛点链
  （同 ModelBar/面板换肤先例），或简化为控件 `QPalette` 取 `placeholderText`
  角色色——选型在实施时取改动面更小者，本计划裁决：**用 QPalette 角色色**，
  零 qss 模板改动、天然随主题（D6）
- radio 粗体、页标题字号等内联 style 集中到模块级 `_STYLE_*` 常量（AFCP 3.4），
  不再散落构建函数（D11）
- 描述缩进 24px 魔法数提取常量 `_DESC_INDENT = 20`（radio 指示器宽 16 + 间距 4），
  按指示器实际度量对齐（D5）

### 2.3 权限页细化（P1）

- 黑名单入口改「`QToolButton` + `▶/▼` 箭头 + 左对齐自适应宽度」，checkable 态
  翻转箭头；不再拉满整行（D2）
- 黑名单只读区 `setFixedHeight(160)` + 等宽字体（`QFontDatabase.systemFont(
  FixedFont)`），条目每行 `编号. 说明` 不变（D13）
- `auto_all` 档警示红色保留（WARNING_COLOR 单一来源不动）

### 2.4 各页布局统一（P1）

五页统一模板：**说明段落 → 控件区 → 补充 hint**，控件区枚举/单选用 VBox、
键值对用 FormLayout 的现行格局保留（两类场景本就不同），但：
- hint 一律走 `_make_hint` + `setWordWrap(True)`，禁止再出现不换行 hint（D8）
- 终端页、高级页控件过少：高级页「打开配置文件 / 恢复默认设置」改水平排列
  + `addStretch(1)` 沉底，视觉重心上移（D9）
- 所有 `_on_*` 槽补 `statusBar().showMessage` 反馈（模型/外观/终端页对齐权限页
  先例），文案统一格式：`{页名}：{项} → {值}`（D12）

### 2.5 明确不做

- 不改 `_PAGE_REGISTRY` 机制本身（仅扩元组字段，注册语义不变）
- 不改任何设置键、默认值、持久化格式
- 不动 MainWindow 应用槽签名与 `_sync_settings_dialog` 链路
- 不动 qss 模板与各主题调色板文件

## 3. 分期实施

| 阶段 | 内容 | 覆盖缺陷 | 预估 |
|:---:|:---|:---|:---:|
| P0 | 骨架重排（页标题区/导航 160px/中文关闭按钮/最小尺寸）+ 样式令牌化（QPalette 角色色、内联 style 收编、缩进常量） | D1 D3 D4 D5 D6 D9(尺寸) D10 D11 | 中 |
| P1 | 权限页细化（箭头折叠按钮、黑名单固定高度等宽字体）+ 各页布局统一（hint 规范、高级页沉底、状态栏反馈补齐） | D2 D8 D9(空页) D12 D13 | 小 |
| P2 | 窗口图标清理（排查标题栏默认图标来源，设置应用级 icon 或移除） | D7 | 小 |

P0/P1 同批交付；P2 涉及应用级图标资源，独立可后置。

## 4. 功能裁剪

- ❌ 导航图标（无图标资源体系，引入 SVG 资源属另一议题，列备案）
- ❌ 设置搜索框（沿 1344 计划裁决：页数 >10 重评）
- ❌ 导航分组（沿 1344 计划裁决：页数 >8 再建）
- ❌ QTranslator 装载中文化全部标准控件（改动面大，仅本对话框用中文按钮即可）
- ❌ 窗口几何持久化（设置中心非主窗口，固定初始尺寸即可）
- ❌ 暗色主题专项截图走查（令牌化后理论上零专项，冒烟覆盖）

## 5. 验证标准

| 项 | 标准 |
|:---|:---|
| 截图复核 | P0/P1 完成后四主题各截「权限页 + 终端页」一张，需求方复核 |
| 页标题 | 切页 → 标题/副标题同步刷新；五页标题与注册表一致 |
| 关闭按钮 | 全中文「关闭」，Esc 同效，无图标 |
| 最小尺寸 | 拖到 600×420 以下被拦截，控件不被裁切 |
| 换肤 | 四主题切换后 hint/副标题颜色跟随 `muted_text` 语义，无灰死 |
| 黑名单 | 折叠按钮箭头随展开态翻转；展开区固定高度 + 等宽字体 |
| 回归 | 现有 `.temp/smoke_settings_dialog.py` 全量过（reload/回弹/缓存用例不受表现层改动影响）；py_compile 全过；ast ≤50 行/≤3 层 |
| 零业务变更 | grep 确认无设置键增删、无 MainWindow 槽签名变更 |

## 6. 风险与对策

| 风险 | 等级 | 对策 |
|:---|:---:|:---|
| 页标题区上移后各页 build 函数删 hint 行遗漏，造成双说明 | 🟢 | P0 完成后逐页截图比对；注册表副标题字段强制非空 |
| QPalette 角色色在部分主题下与 gray 观感差异引发争议 | 🟢 | 四主题截图复核收口，需求方裁决 |
| 注册表元组扩字段触碰既有冒烟断言 | 🟡 | 冒烟脚本同步适配（1344 计划已有此先例） |
| 状态栏反馈补齐致消息刷屏 | 🟢 | 沿用 3000ms 超时先例；连续改同项天然覆盖 |

## 7. 备案

1. 导航图标体系：待项目有统一 SVG/图标资源方案后回填（届时注册表再扩 icon 字段）
2. QTranslator 全局中文化：若未来标准对话框（QMessageBox 按钮等）英文化问题扩散，再单独立项
3. 设置中心窗口尺寸/位置持久化：与 `window_state.py` 主窗口机制同构，有需求再接
4. 若后续页数增长触发 1344 计划备案的导航分组，页标题区天然兼容（标题取页名不取组名）

## 8. 实施记录（2026-07-22 15:35）

### 8.1 交付对照

| 项 | 结果 |
|:---|:---|
| P0 骨架重排 | ✅ 页标题区（注册表扩四元组 `_PAGE_REGISTRY`：(导航名, 副标题, build, reload)，`currentRowChanged` 骨架统一刷新）；导航 140→160px（min/max 160/200 非 Fixed，项内边距 12px + 项高 32 经 `_NAV_ITEM_QSS` 仅覆 padding/min-height，颜色留 app 级 qss）；中文「关闭」按钮替换 QDialogButtonBox；`setMinimumSize(600, 420)` + 初始 680×500；导航右缘 QFrame VLine + 标题下 HLine 双分隔线 |
| P0 样式令牌化 | ✅ hint/副标题/路径标签统一注册 `_hint_labels`，分隔线注册 `_sep_frames`，`apply_theme(theme)` 按 muted_text/border 令牌重刷，挂 `MainWindow.switch_theme` 链（同五面板先例）；`_DESC_INDENT=20` 对齐 radio 指示器；内联 style 收编模块级常量 |
| P1 权限页细化 | ✅ 黑名单入口改 `QToolButton`（左对齐自适应宽度，▶/▼ 箭头随 checkable 翻转）；只读区 `setFixedHeight(160)` + `get_mono_family()`  bundled 等宽字体 |
| P1 各页统一 | ✅ `_make_hint` 一律 `setWordWrap(True)`；高级页双按钮水平排列 + `addStretch(1)` 沉底；状态栏反馈补齐（模型后端/版本、字号、噪音过滤、终端反转；主题切换沿用 MainWindow 既有消息不重复发），文案统一 `{页名}：{项} → {值}`，超时常量改用 `STATUS_MSG_TIMEOUT_MS` |
| P2 窗口图标 | ⏳ 按裁决后置（涉应用级图标资源） |

### 8.2 实施偏差留痕（均为实施期实证后的合理调整）

1. **§2.2 QPalette 裁决被推翻**：核查 `apply_theme()` 只 `setStyleSheet` + `setFont`，**不设 palette**——QPalette 角色色不会随主题变化，裁决前提不成立。改采计划第一选项：muted_text 令牌直读 + `switch_theme` 收敛点链（`SettingsDialog.apply_theme(theme)`，MainWindow 增 3 行挂接，槽签名不变）
2. **导航样式收窄**：核查发现 base.qss `QListView` 段已对导航统一换肤（卡片化 + accent 选中），故内联 qss 仅覆 `padding`/`min-height` 两条物理属性，颜色选择器零覆写（规避 1259 下拉框式 qss 接管教训）
3. **模块 docstring 同步**：注册表格式、apply_theme 收敛点、新增设置项四步流程已更新；§2.5 零业务变更边界守住（无设置键增删、无应用槽签名变更、无主题文件改动）

### 8.3 验证结果

- `.temp/check_compliance.py` 全过（py_compile + ast ≤50 行/≤3 层）
- `.temp/smoke_settings_dialog.py` 全过（新增断言：页标题/副标题随导航刷新、中文关闭按钮唯一、最小尺寸 ≥600×420、黑名单箭头翻转 + 展开区定高、假页四元组注册表分发）
- `.temp/test_permission_policy.py` 26/26 无回归
- 四主题截图复核（`.temp/shot_settings_dialog.py` 产出 8 张存 `.temp/shots/`）：hint 灰随 muted_text 主题间可见差异、导航选中色随 accent（蓝/绿/天蓝/橙）、页标题/分隔线/中文关闭/箭头折叠钮全部正常——四主题均为亮色系，无深色主题，D6 深色对比度风险客观不存在

### 8.4 复核修复轮（2026-07-22 17:00，真实平台走查发现 1 项）

**导航项文字重叠（需求方真实环境截图举证）**：`_NAV_ITEM_QSS` 以 qss
`min-height: 24px` 控导航项高——offscreen（Fusion 样式）截图复核生效，
但 **qss min-height 对 item delegate 的采纳依赖平台 QStyle**，Linux 原生
样式不采纳，项高回落压过字高，相邻项文字互相叠印。教训：offscreen
截图复核无法覆盖平台样式差异，物理尺寸类属性须走跨样式可靠路径。

修复（双连坑全记录）：
1. 项高改 `QListWidgetItem.setSizeHint()`（Qt 官方跨 QStyle 可靠路径），
   qss 收窄为仅覆 `padding`（内边距不决定行高，无平台差异）
2. 首版 `QSize(-1, 32)` 实测不生效——负宽为无效尺寸，`setSizeHint`
   按清除语义把高度一并丢弃（隔离复现证实读回 (-1,-1)）；改
   `QSize(0, 32)` 有效尺寸（宽 0 占位，列表模式项宽由视口决定）

验证：冒烟新增 `item(0).sizeHint().height() == 32` 断言 + 合规 + 四主题
截图复跑全绿。真实平台效果待需求方复走查确认。
