# AI CLI 工具箱说明

> 本文档列出当前 AI CLI（Kimi Code）会话中可用的完整工具清单，供随时翻阅。
> 更新日期：2026-08-01

## 文件与代码操作

| 工具 | 用途 |
|------|------|
| `Read` | 读取文本文件，支持分页、按行号定位、查看文件末尾 |
| `Write` | 创建新文件、覆盖或追加内容，自动创建缺失的父目录 |
| `Edit` | 对已有文件做精确字符串替换，是增量修改的专用工具 |
| `Glob` | 按文件名模式查找文件，如 `**/*.py`、`src/*.ts` |
| `Grep` | 用正则表达式搜索文件内容，支持上下文行、大小写开关 |
| `ReadMediaFile` | 读取图片等媒体文件，用于截图视觉验证闭环 |

## 执行与进程

| 工具 | 用途 |
|------|------|
| `Bash` | 执行 shell 命令，支持后台任务、超时设置、管道与重定向 |
| `TaskList` | 列出所有后台任务及其状态 |
| `TaskOutput` | 查看某个后台任务的输出快照或等待其完成 |
| `TaskStop` | 停止正在运行的后台任务 |

## 子 Agent 与规划

| 工具 | 用途 |
|------|------|
| `Agent` | 启动单个子 agent，类型有 `coder`（可改代码）、`explore`（只读探索）、`plan`（架构规划） |
| `AgentSwarm` | 并行启动多个子 agent，最多 128 个，适合批量任务 |
| `EnterPlanMode` / `ExitPlanMode` | 进入/退出规划模式：先只读探索并产出方案，经批准后再动手 |
| `TodoList` | 维护结构化待办清单，跟踪多步骤任务的进度 |

## 目标模式（自主工作）

| 工具 | 用途 |
|------|------|
| `CreateGoal` | 创建一个跨多轮的长期目标，带可验证的完成条件 |
| `GetGoal` | 查看当前目标的状态与剩余预算 |
| `UpdateGoal` | 将目标标记为继续 / 完成 / 受阻 |
| `SetGoalBudget` | 给目标设置硬预算（轮数、token、时间） |

## 定时任务

| 工具 | 用途 |
|------|------|
| `CronCreate` | 创建定时任务（cron 表达式，本地时区），支持一次性提醒和周期任务 |
| `CronList` | 列出当前所有定时任务 |
| `CronDelete` | 删除指定定时任务 |

## 网络与信息

| 工具 | 用途 |
|------|------|
| `WebSearch` | 联网搜索，返回标题、URL 和摘要 |
| `FetchURL` | 抓取指定网页的正文内容 |

## 交互与技能

| 工具 | 用途 |
|------|------|
| `AskUserQuestion` | 结构化提问，提供多选项供用户选择 |
| `Skill` | 调用已注册技能，当前内置：`check-kimi-code-docs`、`update-config`、`write-goal` |

## 工作规则速记

- 只操作项目目录内的文件，不访问外部路径
- 强制使用项目 `.venv` 的 Python，禁止系统全局 Python
- 临时文件放在项目内（如 `./.temp/`），禁用系统临时目录
- 未获明确许可不执行 `git commit / push / rebase` 等变更操作
- 改动后必须运行验证（测试、构建），再宣告完成

## 能力演示记录

- 2026-08-01：使用 `AgentSwarm` 并行启动 3 个子 agent，在 `子agent演示/` 文件夹中分别生成 `Python简介.md`、`Git简介.md`、`Markdown简介.md`，验证了并行子任务能力。
