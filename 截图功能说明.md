# 命令行截图功能说明

> 本文档供 AI 助手及开发者参考，说明如何通过命令行启动 Zen Studio 并自动截图（用于界面走查）。

## 功能概述

Zen Studio 入口文件 `main.py` 内置了自动截图功能。启动程序时传入对应参数，即可在运行过程中定时抓取主窗口画面并保存为 PNG 图片，方便检查界面渲染效果。

## 命令行参数

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `--auto-screenshot` | 开关 | 关闭 | 启用自动截图（必需） |
| `--screenshot-interval <秒>` | 整数 | 1 | 截图间隔（秒） |
| `--screenshot-on-start` | 开关 | 关闭 | 启动时立即截一张图（延迟 500ms，等待窗口首帧渲染完成） |

## 用法示例

```bash
# 每 1 秒截一张图
uv run main.py --auto-screenshot --screenshot-interval 1

# 启动时先截一张，之后每 5 秒截一张
uv run main.py --auto-screenshot --screenshot-on-start --screenshot-interval 5
```

## 输出说明

- **保存目录**：项目根目录下的 `.tmp/`（不存在时自动创建）
- **文件命名**：`screenshot_YYYYMMDD_HHMMSS_<序号>.png`
- **终端提示**：每次截图会打印一行 `[screenshot] <文件路径>`

## 实现位置

- 参数解析：`main.py` 的 `parse_args()`
- 截图逻辑：`main.py` 的 `setup_screenshot()`，通过 `QTimer` 定时调用 `window.grab().save(...)`

## 注意事项

- 截图功能仅在传入 `--auto-screenshot` 时启用，默认启动不截图。
- 截图内容为主窗口（`MainWindow`）的完整画面，不包含其他顶层窗口/弹窗。
- 截图文件属于临时产物，存放在 `.tmp/` 目录，不应提交到版本库。
