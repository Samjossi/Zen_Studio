# assets/logo候选池/ — Logo 设计草稿存档

历史 Logo 候选方案（6 件 SVG，A–F）的**存档目录**，仅供设计回溯。

## 状态：❌ 不打包、不被代码引用

- 本目录是**设计草稿**，运行时与打包产物均不消费。
- 打包纪律：`building/zen-studio.spec` 的 `datas` 按子目录收编 `assets/fonts` + `assets/logo`，**严禁**把本目录加入 `datas`，也严禁恢复整目录收编 `assets/`。
- 正式 Logo 已定版为 `B_禅圈.svg`（定版资产复制到 `assets/logo/`，母版另有浅色底衬调整）。

## 纪律

- 本目录文件**只存档、不改动**；换标走 `assets/logo/` 的母版 + 重渲染流程，不动候选池原件。
- 新增候选方案可直接放入本目录，命名沿用「字母_主题.svg」格式。
