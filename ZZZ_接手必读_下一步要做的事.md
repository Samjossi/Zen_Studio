> **状态**：进行中（待实施）
> **范围**：中栏文件查看器（`gui/panels/viewer/`）可预览文件类型扩展
> **时间**：2026-07-25 20:45 (UTC+8)
> **优先级**：见 §3 优先级表

---

# ⚡ ZZZ —— 接手必读（下次开工先读本文件！）

> 👋 **你好，下一位接手 Zen Studio 的工程师 / AI：**
>
> 请**第一时间通读本文件**。它汇总了当前阶段要推进的工作全貌、四份选型决策文档的入口、推荐的推进顺序，以及贯穿全部子任务的共享架构。读完本文，再按 §3 顺序点开对应的选型文档进入实施即可。
>
> 本文件以 `ZZZ_` 前缀命名，目的就是让它在文件列表里**沉到最底部、最显眼**，确保你一眼就能找到"接下来该干嘛"。

---

## 1. 这一批要做什么（一句话总览）

把 Zen Studio 中栏文件查看器从「**只能看文本**」扩展为「**文本 / 图片 / PDF / Markdown 渲染 / 音视频**」多类型就地预览。当前 `.svg`、`.png`、`.pdf`、`.md`（渲染）、`.mp4`、`.mp3` 等都会落入"（二进制文件，不支持预览）"占位分支——本批次就是补齐这些能力。

---

## 2. 四份选型文档（实施前必读入口）

> 以下四份文档位于 `work options/` 目录，已**完成选型论证**，记录了背景、现状、候选方案对比、推荐结论与实施要点。按你接下来要做的类型对号入座阅读。

| 顺序 | 文档（相对路径） | 能力 | 选定方案 |
|:---:|:---|:---|:---|
| ① | `work options/2026-0725-1849_图片文件预览功能选型.md` | 图片预览（PNG/JPG/GIF/WebP/BMP/SVG…） | `QGraphicsView` + 矢量 SVG（**本系列主篇，接入架构奠基**） |
| ② | `work options/2026-0725-2027_PDF文件预览功能选型.md` | PDF 就地预览 | QtPdf（`QPdfView` + `QPdfDocument`） |
| ③ | `work options/2026-0725-2027_Markdown渲染预览功能选型.md` | Markdown 渲染视图 | `QTextBrowser.setMarkdown`（GFM 方言） |
| ④ | `work options/2026-0725-2027_音视频播放功能选型.md` | 视频/音频播放 | QtMultimedia（`QMediaPlayer`）**优先级最低，建议暂缓** |

---

## 3. 优先级与推荐推进顺序

| 优先级 | 子任务 | 备注 |
|:---:|:---|:---|
| 🟢 **中（先做）** | ① 图片预览 | **接入架构的主篇**，先做它，QStackedLayout 分流基建一次性建好，后续类型只往里加页 |
| 🟢 **中** | ② PDF 预览 | 复用图片选型建好的分流基建；⚠️ 首要风险是**打包态 QtPdf 插件收编**，需 onedir/AppImage 实机验证 |
| 🟢 **中** | ③ Markdown 渲染 | 源码视图完全复用现有 `CodeViewer`，零改动；只新增渲染视图页 + 双模式切换 |
| 🟡 **低（最后/可暂缓）** | ④ 音视频播放 | 场景价值最低；⚠️ 最大不确定性是**打包态媒体后端解码**。建议等 ①②③ 落地后再评估是否真有需求 |

> **推荐路线**：① 图片（奠基） → ②③ PDF / Markdown（可并行，都只是往分流基建里加一页） → ④ 音视频（评估后决定做不做）。

---

## 4. 贯穿全部子任务的共享架构（关键）

四个子任务**共用同一套接入架构**，理解这一点能避免重复劳动：

- **接入点**：`gui/panels/viewer/panel.py` 的 `ViewerPanel.open_file(path)`。
- **分流方式**：在 `open_file` 开头**按文件类型分流**（文本 → `CodeViewer`，图片 → image_viewer，PDF → PdfView，Markdown → MarkdownView，媒体 → media_viewer，二进制 → 占位）。
- **切换机制**：用 `QStackedLayout` 在同一面板内切换不同控件页，**复用现有面板壳**（标题行、`QFileSystemWatcher` 外部修改自动重载、Git 差异徽标、查找浮层）。
- **依赖底牌**：全部选定方案均基于 `pyside6>=6.11.1` 自带模块（`QGraphicsView`/`QSvgWidget`、`QtPdf`/`QtPdfWidgets`、`QTextBrowser.setMarkdown`、`QtMultimedia`/`QtMultimediaWidgets`），**零新增第三方依赖**。
- **统一否决项**：四个选型**一致否决 `QWebEngineView`**（与项目既定的轻量哲学冲突，见各选型文档及 `文档/选型记录/` 下相关报告）。

> 💡 实施时建议：先在图片选型里把 `QStackedLayout` + `open_file` 扩展名分流这套基建做扎实，后续 PDF/Markdown/音视频都是"加一个控件 + 加一个分流分支"，工作量递减。

---

## 5. 每个子任务实施时务必注意的风险点（速查）

| 子任务 | ⚠️ 关键风险 / 注意 |
|:---|:---|
| ① 图片 | 超大图防护（参考 VS_Code_Python 的 32768px/80MP 钳制）；SVG 矢量渲染（`QGraphicsSvgItem`）；透明图棋盘格底 |
| ② PDF | 🔴 **打包态 QtPdf 插件收编**——开发态 import 通过 ≠ 打包态能渲染，必须 onedir/AppImage 实机验证 |
| ③ Markdown | 相对图片/链接需以 md 文件所在目录为基准解析（`setSearchPaths`）；能力上限由 Qt 富文本引擎决定（不支持代码块语法高亮 / Mermaid / LaTeX） |
| ④ 音视频 | 🔴 **打包态媒体后端解码**——开发态能播 ≠ 打包态能播，必须实机验证 mp4/mp3；切换文件时务必 `stop()` 释放播放器 |

---

## 6. 接手后的标准动作清单

1. ✅ 读完本文件（你正在做）。
2. ✅ 读 `AGENTS.md`（项目操作约束：仅操作项目目录内文件、强制用 `.venv` 的 Python、临时文件放项目内、中文响应）。
3. ✅ 读 `文档编写规范.md`（文档命名、引用、符号约定——尤其 §4：文件引用一律反引号纯文本，禁止 Markdown 链接）。
4. ▶️ 按本文 §3 推荐顺序，点开对应选型文档，阅读其"实施要点"章节后立项（`work plans/`）或直接动手。
5. 🔎 实施前先看一眼 `gui/panels/viewer/panel.py` 与 `gui/panels/viewer/code_viewer.py` 的现状，确认接入点未发生变动。

---

*撰写于 2026-07-25 20:45 (UTC+8) | 汇总自：图片 / PDF / Markdown / 音视频 四份选型文档*
